# Directives Système et Règles de Cohérence du Projet

Ce fichier contient des instructions persistantes destinées aux assistants IA de Google AI Studio pour maintenir la cohérence de l'application, éviter les régressions et respecter le travail existant.

---

### 1. 🛡️ Préservation de l'Architecture & Code Existant
- **Pas de réécriture inutile :** Ne jamais recréer ou dupliquer des classes de données, des DAOs, ou des fonctions utilitaires existantes. Toujours réutiliser le `InventoryViewModel` et `InventoryRepository`.
- **Analyse avant modification :** Il est **obligatoire** de lire l'intégralité d'un fichier avec `view_file` avant d'y appliquer des modifications. Les modifications doivent être ciblées et chirurgicales (utiliser `edit_file` ou `multi_edit_file` au lieu de réécrire des fichiers entiers).
- **Compilation régulière :** Valider chaque étape de modification en exécutant `compile_applet`. Ne jamais laisser l'application dans un état non compilable.

### 2. 🎨 Cohérence Visuelle & Thématique (Material Design 3)
- **Thème et Couleurs (Mis à jour) :** L'application intègre le thème officiel avec les palettes de couleurs suivantes :
  - *Mode Clair (Light) :* Primaire (`0xFF012D1D`), Secondaire (`0xFF855300`), Arrière-plan/Surface (`0xFFFBF9F8`), Conteneurs de surface (`0xFFEFEDED`).
  - *Mode Sombre (Dark) :* Primaire (`0xFFC1ECD4`), Secondaire (`0xFFFEA619`), Arrière-plan/Surface (`0xFF002114`), Conteneurs de surface (`0xFF1B4332`).
  - Utiliser exclusivement le système de couleurs dynamiques et les variables de thème fournies par `MaterialTheme.colorScheme` ou `viewModel.themeColor` (qui par défaut mappe vers le nouveau vert sombre `0xFF012D1D`). Ne jamais utiliser de codes couleurs hexadécimaux bruts dans les Composables.
- **Typographie Officielle :**
  - Titres et En-têtes : Police **Epilogue** (Gras, Semi-Gras, Moyen).
  - Corps de texte et Badges : Police **Be Vietnam Pro** (Normal, Moyen, Semi-Gras).
- **Icônes Consistantes :** Pour l'icône principale de l'application (le panier d'achat), toujours utiliser `Icons.Rounded.ShoppingBasket` (et non `Icons.Default.ShoppingBag`) afin d'assurer une cohérence parfaite entre l'écran d'accueil, le splash screen et l'historique des ventes.
- **Espacement et Paddings :** Respecter une grille de 8dp constante. Préférer des marges généreuses et des arrondis de cartes de `12.dp` à `16.dp` pour un rendu moderne et aéré.

### 3. 🌐 Prise en charge Multilingue (FR / MG)
- L'application prend en charge le Français (`fr`) et le Malgache (`mg`).
- Lors de l'ajout de nouvelles interfaces ou de nouveaux boutons, toujours récupérer la langue active depuis `viewModel.language` et fournir des textes traduits pour les deux langues (ainsi que l'anglais par défaut).
- Exemple :
  ```kotlin
  val title = when (activeLang) {
      "mg" -> "Fampidirana entana"
      "fr" -> "Approvisionnement"
      else -> "Restocking"
  }
  ```

### 4. 🚀 Simplicité et Focus Fonctionnel
- Ne pas ajouter de fonctionnalités non demandées explicitement par l'utilisateur (over-engineering).
- S'en tenir strictement au besoin décrit en se concentrant sur une expérience utilisateur fluide, rapide et exempte de bugs.
- Conserver le stockage local via la base de données Room préexistante pour toutes les entités importantes (produits, ventes, dettes, fournisseurs).

### 4bis. 🔒 Branche de Travail et Interdiction de Pousser sur `main` (MANDATORY)

**Tout le développement part sur la branche de travail** (`claude/...`), jamais directement sur
`main`. Une fusion vers `main` n'a lieu QUE sur autorisation explicite de l'utilisateur, demandée
à chaque fois — une autorisation donnée pour un lot ne vaut pas pour le suivant. Le déroulé
attendu est : commit + push sur la branche de travail → attendre la CI (`verification.yml`) →
annoncer le résultat → demander s'il faut fusionner. C'est aussi ce qui protège `main`, sur lequel
`release.yml` construit et publie automatiquement l'APK signé de la Release GitHub : y pousser,
c'est livrer au client.

### 5. 📝 Documentation Continue et Maintien du Fichier AGENTS.md (MANDATORY)
- **Mise à jour systématique :** À chaque ajout, modification ou suppression d'une fonctionnalité ou d'un écran, l'assistant IA **DOIT** impérativement mettre à jour ce fichier `AGENTS.md` dans la section **"6. 🗺️ Carte des Fonctionnalités de l'Application"** ci-dessous pour refléter l'état réel et actuel du projet. Cela garantit une transmission parfaite du contexte aux futurs assistants.

---

### 6. 🗺️ Carte des Fonctionnalités Actuelles de l'Application

Voici l'inventaire officiel des fonctionnalités implémentées dans l'application :

1. **Écran de Caisse & Calculatrice (`CalculatorScreen` / `HomeScreen` Grille)**
   - Calculatrice interactive pour saisir des montants manuels ou ajouter des produits du catalogue directement au panier.
   - Gestion des types de produits (vendu par unité standard, liquide en Litres, ou poids en Kilogrammes avec boutons pré-réglés `1/4 kg`, `1/2 kg`, `+1`).
   - Saisie rapide par code-barres via caméra hautement optimisée à très faible latence.
   - Validation de la vente avec enregistrement automatique dans la base de données Room, mise à jour instantanée des stocks, et avertissement si stock insuffisant.
   - **Badge de clic / Quantité & Bouton de décrémentation (Mise à jour) :** Chaque carte de produit sur l'écran d'accueil affiche un badge dynamique coloré dans le coin supérieur droit de son image montrant le nombre de fois qu'elle a été cliquée (quantité active dans le panier). Ce badge intègre un bouton `-` interactif à faible encombrement permettant d'annuler/décrémenter directement un clic sans avoir à ouvrir le panneau du panier.
   - **Design Visuel Purifié (Nouveau) :** Retrait complet des badges de catégories sur les cartes de l'écran d'accueil, avec augmentation de la hauteur de l'image de 130dp à 165dp pour un rendu visuel moderne et immersif. Chaque fiche présente de manière épurée et aérée uniquement le nom du produit, le total en stock (badge de couleur douce) et le prix unitaire d'un seul coup d'œil.

2. **Écran de Gestion de Stock & Inventaire (`InventoryListScreen` / `AddProductScreen`)**
   - Liste des produits avec recherche instantanée et filtrage par stock faible.
   - **Retour à la ligne automatique des catégories :** Utilisation de `FlowRow` pour afficher la catégorie, le SKU et les badges de statut (ALERT, LOW QTY) afin d'éviter tout écrasement ou distorsion de la mise en page lorsque les noms de catégories sont longs.
   - Ajout ou édition complète d'un produit (Nom, Prix de vente, Prix d'achat de base, Unité, Code-barres, Seuil d'alerte de stock faible, Fournisseur attitré).
   - Suppression sécurisée et ajustement manuel rapide des quantités en stock.
   - **Filtre de Modèles Secteur-Neutre (Nouveau) :** Les produits pré-configurés (modèles d'épicerie) sont désormais entièrement masqués des listes de vente, de stock et de recherche par défaut. Cela permet de conserver l'application parfaitement propre et prête pour n'importe quel autre secteur d'activité (poissonnerie, bar, etc.).
   - **Ajout rapide depuis un Modèle (Nouveau) :** Bouton d'accès direct dans le formulaire de création de produit pour ouvrir un sélecteur de modèles. L'utilisateur peut y rechercher et filtrer par catégorie des centaines de produits pré-enregistrés. Sélectionner un modèle pré-remplit instantanément et localement la fiche produit (Nom, Catégorie, Unité, SKU), lui laissant simplement à saisir le prix de vente et les stocks restants.
   - **Génération & Impression de Code-barres Vectoriels (Nouveau) :** Génération automatique et aléatoire de numéros de code-barres uniques (Code 39) en un clic. Création automatique en arrière-plan d'une étiquette PDF au format vectoriel haute résolution incluant le nom du produit et le code-barres, sauvegardée directement dans le dossier public `Downloads/EpicerieBarcodes`. Lancement instantané du flux d'impression Android standard (`PrintManager`) pour une impression rapide sur des étiquettes adhésives.
   - **Intégration Open Food Facts API (Nouveau) :** Module de recherche mondiale de produits intégré permettant de scanner un code-barres ou de saisir un mot-clé pour pré-remplir instantanément la fiche produit. Récupère automatiquement le nom, la marque, la description, la catégorie (mappée intelligemment vers nos sections Alimentation, Boissons, Légumes, Épicerie, Droguerie, Hafa) ainsi que l'image miniature officielle du produit via Coil. L'utilisateur n'a plus qu'à saisir son prix de vente final.

3. **Écran d'Historique & Traçabilité des Transactions (`SalesHistoryScreen`)**
   - **Système d'Onglets d'Historiques :** Permet de basculer instantanément entre l'Historique des Ventes (**"Varotra"**) et l'Historique d'Approvisionnement (**"Fampidirana"**) pour une traçabilité totale et absolue de toutes les opérations financières et de stock.
   - **Correction et Visualisation :** Affiche le chiffre d'affaires de la journée sous le terme exact de **"Vola maty androany"** (ou "Vola maty tamin'io daty io" si une autre date est choisie) de manière entièrement dynamique.
   - **Filtre par calendrier haute précision :** Sélecteur de date interactif à icône de calendrier permettant d'isoler et d'analyser les transactions d'une journée spécifique (mettant à jour instantanément les métriques de revenus et le nombre d'opérations).
   - **Barre de recherche dynamique et fluide :** Barre de recherche s'activant par glissement pour filtrer les ventes par nom de produit et les réapprovisionnements par nom de produit ou fournisseur en temps réel.
   - **Kajy momba ny Vola (Analyses Financières Réelles) :** Un clic sur l'icône de diagramme à barres (BarChart) ouvre un tableau d'analyse consolidé calculant le nombre total de ventes, le chiffre d'affaires, le coût d'achat des produits vendus (COGS), le bénéfice net de la journée et le taux de marge réel.
   - Possibilité d'annuler/supprimer n'importe quelle vente ou réapprovisionnement de l'historique avec confirmation de sécurité.

4. **Écran de Gestion de Dettes (`DebtsScreen`)**
   - Enregistrement et suivi des clients ayant des arriérés de paiement.
   - Remboursement partiel ou total des dettes avec mise à jour du solde dû.
   - Suppression ou extinction de dettes.
   - Fusion visuelle automatique des dettes d'un même débiteur (nom identique, insensible à la casse/aux espaces) en une seule carte affichant le total dû — voir section 31.

5. **Écran de Paramètres & Options (`SettingsScreen`)**
   - Changement instantané de la langue de l'interface (Français `fr` ou Malgache `mg`).
   - Personnalisation du thème de couleur principal (Bleu, Vert, Orange, Violet, etc.).
   - Choix du mode d'affichage thématique (Sombre, Clair ou adaptation automatique selon le Système) pour une visibilité idéale de jour comme de nuit.
   - Modification du nom de l'épicerie affiché en haut de l'écran d'accueil.
   - Bouton de redirection vers le calculateur d'approvisionnement.
   - Bouton de redirection vers l'écran de gestion et impression consolidée de codes-barres.

6. **Écran de Calculateur d'Approvisionnement & Marges (`CommissionScreen`)**
   - Accessible depuis la page des Paramètres.
   - **Tutoriel & Onboarding intégré :** Intègre une carte d'astuces "💡 Mode d'emploi / Fampiasana" décrivant précisément l'ordre idéal de saisie pour guider pas à pas l'utilisateur (choix de produit, cartons, pièces, coût d'achat total, prix de vente).
   - **Calculateur d'achat & Bouton "Effacer tout" :** Permet d'entrer un produit, le nombre de cartons/lots achetés chez un fournisseur, le nombre de pièces par carton, et le coût total d'achat en gros. Un bouton rapide "Hamafana / Effacer tout" permet d'effacer d'un seul clic les données de démonstration pour y saisir de nouvelles valeurs personnalisées.
   - **Analyse de marge automatique :** Calcule de manière robuste et sans plantage (en gérant élégamment les champs vides) le coût unitaire réel, le bénéfice total généré, le bénéfice par carton, le bénéfice par unité, et le taux de marge d'après le prix de vente unitaire saisi (que l'utilisateur peut ajuster en direct sur cet écran).
   - **Badge de Sécurité de Marge Dynamique (Nouveau) :** Affiche un badge coloré dynamique au sommet du panneau d'analyse pour alerter instantanément le gérant sur la rentabilité de sa tarification : Fatiantoka/Perte (Rouge) ⚠️, Tombony kely/Marge faible (Jaune) ⚠️, ou Tombony tsara/Marge excellente (Vert) ✨.
   - **Validation du réapprovisionnement :** Met à jour directement le stock du produit en y ajoutant les nouvelles unités reçues, enregistre le nouveau prix d'achat/gros et le nouveau prix de vente, et lie le fournisseur choisi au produit.
   - **Alerte de stock faible (Tahiry ho lany) :** Un deuxième onglet qui liste automatiquement tous les produits dont le niveau de stock est inférieur au seuil configuré, avec un bouton d'accès rapide "Approvisionner" pour les charger directement dans le calculateur.
   - **Ajout rapide de fournisseurs :** Formulaire en boîte de dialogue pour créer instantanément de nouveaux fournisseurs sur place si non listés.
   - **Traduction parfaite et harmonisée :** Toutes les lignes de calcul de marges, de coûts de revient et de bénéfices unitaires/globaux sont entièrement localisées avec soin en Malgache (MG), Français (FR) et Anglais (EN).

7. **Écran de Gestion de Codes-Barres (`BarcodeListScreen`)**
   - Accessible depuis la page des Paramètres.
   - **Filtres par onglets tactiles :** Sépare d'un clic les produits avec code-barres et sans code-barres pour un suivi instantané de l'état de l'inventaire.
   - **Recherche temps réel & Groupement par Catégorie :** Filtrage instantané des produits par nom ou numéro de code-barres. Les produits sont triés et groupés dynamiquement par catégorie (par exemple : Alimentation, Boisson, etc.) avec des bannières de titre de section colorées et élégantes.
   - **Génération unifiée en arrière-plan :** Pour les produits sans code-barres, un clic sur le bouton "Générer" attribue dynamiquement un code unique de 13 chiffres intégrant une clé de contrôle (checksum) conforme aux normes internationales EAN-13, et met à jour instantanément la base de données locale.
   - **Impression consolidée intelligente (`varotra_code_barre.pdf`) :** Génère un document PDF unique regroupant l'ensemble des codes-barres générés par l'application sous forme de planche d'étiquettes vectorielles haute résolution disposées en grille de 2 colonnes (5 lignes par page) pour un scan optimal. Les produits y sont regroupés et ordonnés par catégorie.
   - **Formes de codes-barres optimisées (EAN-13 Standard de Qualité) :** Les barres de garde (début, milieu, fin) s'étendent élégamment vers le bas à travers la zone de texte, et les chiffres sont espacés et présentés de manière conforme aux standards industriels sous la planche pour un scan instantané à la caméra ou avec un lecteur optique.
   - **Saut de page automatique sans coupe :** Intègre une logique géométrique stricte de saut de page (dès 10 étiquettes atteintes par planche), garantissant qu'aucune étiquette ne soit coupée horizontalement entre deux pages.
   - **Impression unitaire :** Possibilité de relancer individuellement l'impression d'un produit précis depuis sa fiche avec le même rendu EAN-13 haute fidélité.

8. **Écran de Synchronisation Multi-terminal (`SyncScreen`)**
   - Accessible depuis la page des Paramètres.
   - **Mode Serveur (Gérant) :** Permet d'héberger une base de données de synchronisation sur le réseau local Wi-Fi. Affiche un code QR contenant l'adresse IP locale du serveur pour faciliter l'association instantanée des clients, inclut un bouton de redirection rapide vers les paramètres de Point d'accès (Hotspot) du téléphone pour faciliter le partage réseau direct, et montre le nombre de terminaux connectés en temps réel.
   - **Mode Client (Vendeur) :** Intègre un scanner de code QR dédié et hautement optimisé (`QrCodeScannerView`) filtrant exclusivement les formats QR Code (évitant tout conflit avec les codes-barres standards) et incluant un raccourci d'accès direct aux paramètres Wi-Fi de l'appareil ainsi qu'une interface de saisie manuelle de l'adresse IP en secours, permettant de lire l'adresse IP du serveur et s'y connecter instantanément.
   - **Mise à jour en temps réel & Synchronisation feno bidirectionnelle (Mise à jour) :** Permet la synchronisation bidirectionnelle fluide et automatique de toute la base de données (produits avec leurs images, ventes, dettes, approvisionnements/restocks) dès la connexion et lors de chaque transaction ou modification de stock. Résout définitivement le décalage de quantité en empêchant le double-décrément de stock lors du traitement des transactions synchronisées.
    - **Sécurité et Contrôle de Suppression :** Restreint strictement la suppression d'un produit aux appareils définis comme Serveur (Gérant) ; toute tentative de suppression initiée par un appareil Client est bloquée et signalée par un avertissement clair pour préserver l'intégrité de la base de données.
   - **Forçage manuel de synchronisation :** Ajout de boutons physiques "Forcer la synchronisation" (Ampitoviana mivantana ny tahiry) sur les écrans Client et Serveur pour déclencher un cycle de consolidation complet à la demande.
   - **Console de Journalisation (Logs) :** Fournit un affichage temps réel de tous les événements de synchronisation avec un code couleur spécifique (Vert pour les succès, Rouge pour les erreurs) pour faciliter le débogage.

    - **Gestion de Synchronisation Sélective (Nouveau) :** Ajout d'un bouton de synchronisation directe dans la barre d'outils supérieure (`TopAppBar`) à côté des paramètres. Il ouvre un dialogue de contrôle de synchronisation en temps réel qui permet de rechercher n'importe quel produit et d'activer/désactiver sélectivement sa synchronisation. Un bouton de suppression ("fako") permet d'annuler sa synchronisation à tout moment (le produit reste local mais n'est plus propagé sur le réseau), avec affichage de badges clairs d'état de synchronisation ("Mampitovy" / "Tsy ampitoviana").

9. **Sécurisation & Backup Automatique de la Base de Données (Nouveau)**
   - **Protection anti-effacement automatique :** L'application intègre un système d'auto-sauvegarde automatique en arrière-plan. À chaque transaction, modification de produit, paiement de dette, ou fampitoviana (synchronisation) réussie, la base de données consolidée est sérialisée en JSON et sauvegardée dans un fichier local sécurisé (`database_safety_backup.json`) au sein de l'espace privé de l'application.
   - **Restauration transparente automatique :** Si la base de données venait à être effacée ou réinitialisée lors d'une mise à jour de version de l'application (changement de schéma ou version Room sans migration), l'application détecte automatiquement la perte de données au démarrage et réinjecte instantanément la totalité des produits, ventes, et dettes depuis la sauvegarde de sécurité locale.
   - **Contrôles manuels dans les Paramètres :** Ajout d'une section dédiée "Fiarovana ny Tahiry" (Sauvegarde & Sécurité) dans l'écran des paramètres permettant à l'utilisateur de déclencher à tout moment un backup manuel ou de forcer une restauration complète de ses enregistrements professionnels.
   - **Sauvegarde Cloud via Firebase Realtime Database (Mis à jour) :** Ajout d'une section "Sauvegarde Cloud (Firebase)" dans les Paramètres, juste sous la sauvegarde locale, permettant d'envoyer/récupérer la même sauvegarde JSON consolidée (produits, ventes, dettes, réappros, mouvements de caisse) vers/depuis Firebase Realtime Database. Implémenté via un client REST léger (`FirebaseBackupManager`, OkHttp) plutôt que le SDK Firebase ou le plugin Gradle `google-services`, afin que la compilation ne soit jamais bloquée par une configuration Firebase absente. Realtime Database est utilisé plutôt que Cloud Storage car ce dernier nécessite désormais le plan payant Blaze (depuis fin 2024), alors que Realtime Database reste gratuit sur le plan Spark sans compte de facturation. L'URL de la base se configure directement dans l'écran (aucune reconstruction de l'application nécessaire), sur le même principe que la saisie manuelle d'IP du module de synchronisation.

10. **Architecture, Optimisation et Performance (Nouveau)**
    - **Indexation Room Database :** Ajout d'un index sur `isTemplate` dans l'entité `Product` (en plus de `name`, `category`, `barcode`, `sku`) pour optimiser à l'extrême les temps d'accès lors de la recherche et filtrage de modèles de produits.
    - **Algorithme Prédictif Haute-Performance :** Implémentation d'un système de recommandation double-moteur (Co-occurrences transactionnelles tirées de l'historique et repli sémantique par mots-clés localisés). Toutes les fusions et tris de classement de pertinence s'exécutent de manière asynchrone sur le pool de threads `Dispatchers.Default` via des liaisons `combine` réactives, assurant des calculs fluides et instantanés même avec des catalogues volumineux sans jamais ralentir le fil d'exécution principal (UI thread).
    - **Optimisation de Rendu Jetpack Compose :** Configuration systématique de clés uniques stables (`key`) dans les listes et grilles de l'application (`HomeScreen` produits, `CalculatorScreen` produits et paniers, `InventoryListScreen` produits et puces de catégorie, `DebtsScreen` dettes, `SalesHistoryScreen` transactions) pour réduire à zéro les recompositions redondantes et éliminer les micro-saccades (jank) lors du défilement ou des mutations.
    - **Optimisation Mémoire & Images (Coil) :** Configuration globale de `ImageLoader` avec limite de cache mémoire (15%) et disque (2%) pour éviter les OutOfMemoryErrors sur les téléphones bas de gamme. Application systématique du downsampling (`.size()`), de fondus (`crossfade`) et de `ColorPainter` en tant que placeholders de secours pour les milliers d'images distantes du catalogue de produits.
    - **R8 / ProGuard Minification & Réduction d'Espace :** Activation de `isMinifyEnabled = true` et `isShrinkResources = true` dans le profil de publication Gradle. Configuration de règles chirurgicales robustes dans `proguard-rules.pro` pour garantir que Room, Moshi, Retrofit, OkHttp et les modèles de données ne subissent aucune régression de sérialisation par réflexion tout en maximisant la compression.
    - **Compatibilité Multi-Architecture (ABIs) :** Configuration optimisée de la compilation native pour supporter de manière optimale tous les processeurs majeurs (`arm64-v8a`, `armeabi-v7a`, `x86`, `x86_64`) assurant une exécution fluide et rapide des modules ML Kit et SQLite sur l'ensemble du parc d'appareils Android.
    - **Suppression en Cascade Sécurisée (Nouveau) :** Configuration de la contrainte `onDelete = ForeignKey.CASCADE` sur les clés étrangères des lignes de ventes (`lignes_vente`) référençant un produit ou une unité, éliminant définitivement les erreurs de violation d'intégrité référentielle (`SQLiteConstraintException`) et les crashs de l'application lors de la suppression de produits.
    - **Optimisation du Cold Start (Écran Blanc/Gris) (Nouveau) :** Intégration d'un arrière-plan de fenêtre personnalisé (`android:windowBackground`) lié à la couleur primaire de la marque (`#012D1D`) au niveau du thème de démarrage Android de l'activité principale. Cela élimine complètement les 3 secondes d'écran gris d'initialisation du système d'exploitation, assurant un affichage instantané et fluide du Splash Screen de la caisse.
    - **Système d'Activation Sécurisé (Nouveau) :** Intégration d'un écran d'activation obligatoire à la première ouverture de l'application (`ActivationScreen`). L'activation utilise l'identifiant unique de l'appareil (`Installation ID`) avec un bouton de copie au presse-papiers intégré en un clic, et applique la formule mathématique ultra-sécurisée `(I * 3 + 123456) mod 1000000` pour valider le code de déverrouillage permanent à 6 chiffres.
    - **Démarrage Ultra-Rapide et Optimisation Asynchrone (Nouveau) :** Déplacement de toutes les opérations de démarrage (vérification de base, chargement des sauvegardes et initialisation des templates de produits) vers le pool asynchrone `Dispatchers.IO` pour éliminer tout risque de blocage du thread UI principal. Les opérations d'écriture pour les centaines de produits d'import d'épicerie ont été optimisées chirurgicalement pour court-circuiter les synchronisations relationnelles inutiles lors de l'insertion de fiches de modèles (templates), réduisant de ce fait le délai d'affichage du Splash Screen à 1 seconde tout en conservant une fluidité absolue.
    - **Sécurisation Anti-blocage des DAOs (Nouveau) :** Toutes les requêtes de base de données retournant des listes ou des ensembles de données ont été configurées sous forme de flux réactifs asynchrones (`Flow<List<T>>`) et isolées explicitement sur le pool de threads I/O via l'opérateur `.flowOn(Dispatchers.IO)`. De même, l'ensemble des opérations d'écriture, de suppression et d'accès ponctuel ont été enveloppées de manière sécurisée dans des blocs `withContext(Dispatchers.IO)` au sein d'un `InventoryRepository` réfacteuré. Cette isolation absolue garantit qu'aucune opération d'E/S (I/O) ne puisse s'exécuter sur le thread principal de l'interface utilisateur, éliminant tout risque de blocage d'interface (Jank/ANR).
    - **Gestion d'Erreurs Centralisée des Coroutines (Nouveau) :** Intégration de `CoroutineExceptionHandler` au sein de `SyncManager` (`mainScope`), `SyncService` (`serviceScope`) et `InventoryViewModel` (`viewModelScope`) pour intercepter et consigner de manière centralisée toutes les exceptions non gérées, évitant ainsi les plantages inopinés de l'application sans traces de débogage.
    - **Gestion Propre du Cycle de Vie et Annulation (Nouveau) :** Validation de l'utilisation rigoureuse de `viewModelScope` et implémentation explicite de `onCleared()` pour journaliser la fin du cycle de vie du ViewModel, garantissant la destruction propre et l'annulation automatique de toutes les tâches coroutines associées lors du démontage de l'écran.

11. **Fiabilisation CameraX, ML Kit et Autorisations (Nouveau)**
    - **Gestion Sûre du Cycle de Vie (Lifecycle Event Observer) :** Liaison robuste de l'exécuteur de la caméra (`Executors.newSingleThreadExecutor()`) au cycle de vie de `lifecycleOwner` via un `LifecycleEventObserver`. L'exécuteur s'arrête systématiquement et proprement (`shutdown()`) lors du démontage (`ON_DESTROY`) ou de la destruction de l'écran (navigation retour, rotation d'écran), éliminant tout risque de fuite de mémoire.
    - **Fermeture Garantie de l'ImageProxy :** Encapsulation complète de la logique d'analyse d'image dans un bloc `try-catch` robuste au sein des analyseurs de `BarcodeScannerView` et `QrCodeScannerView`. Cela garantit que `imageProxy.close()` soit inconditionnellement exécuté (que la détection réussisse, échoue ou lève une exception), prévenant ainsi définitivement toute saturation de mémoire ou blocage du flux vidéo de la caméra.
    - **Expérience d'Autorisations Interactive & Bilingue :** Amélioration de la gestion de l'autorisation `CAMERA` en cas de refus. Au lieu de laisser l'utilisateur bloqué sur un écran d'attente passif, un bouton d'action contextuel clair ("Autoriser l'appareil photo" / "Omeo fahazoan-dalana") est proposé au centre de l'overlay de scannage pour relancer instantanément la boîte de dialogue système de demande de permission.

12. **Gestion de Caisse : Entrées et Sorties Manuelles (`CaisseMouvementsScreen`) (Nouveau)**
    - **Journal des mouvements d'espèces :** Nouvel écran accessible depuis les Paramètres ("Mouvements de caisse") permettant d'enregistrer un apport d'espèces (ex : fond de caisse) ou un retrait (ex : dépense, retrait du gérant) avec motif (suggestions rapides + saisie libre), montant, note optionnelle et date.
    - **Solde de caisse théorique :** Carte d'en-tête affichant en temps réel le solde de caisse calculé comme ventes totales + entrées manuelles − sorties manuelles, avec liste chronologique de tous les mouvements et suppression individuelle sécurisée par confirmation.
    - **Nouvelle entité Room `MouvementCaisse`** (+ `MouvementCaisseDao`), intégrée à `InventoryRepository`/`InventoryViewModel`, à la sauvegarde de sécurité locale, à la sauvegarde Cloud Firebase et à la synchronisation multi-terminal (`SyncSerializer`) au même titre que les produits, ventes, dettes et réappros.

13. **Correctifs de Fiabilité (Nouveau)**
    - **Suppression de code mort dangereux :** Retrait de `InventoryRepository.insertSale()`, une copie non-transactionnelle de `checkoutSale()` jamais appelée par l'application et qui ne synchronisait pas le stock legacy `Product`, éliminant un risque de désynchronisation du stock si elle avait été utilisée par erreur.
    - **Correction du badge de stock obsolète :** Le badge "LOW QTY" et le texte "Qty:" de `InventoryListScreen` lisaient `product.stock_quantity`, un compteur jamais décrémenté lors d'une vente (seul `product.stock` l'est), affichant donc une quantité figée depuis la création du produit. Ces affichages utilisent désormais la valeur réelle et à jour `product.stock`.
    - **Clés de recomposition additionnelles :** Ajout de `key` stables sur les listes `productsWithBarcodes` et `miscCartItems` de `CalculatorScreen`, ainsi que `templatesList` de `AddProductScreen`, complétant la couverture déjà en place ailleurs dans l'application (section 10).
    - **Génération Code 39 sécurisée :** `BarcodeUtil.generateCode39Pattern()` (table limitée aux chiffres/espace/`*`) rejette désormais tout code contenant un caractère non représentable au lieu de l'imprimer tronqué (silencieusement différent de la valeur réelle stockée).
    - **Retour scan sans correspondance :** En mode scan panier continu, un code-barres scanné sans produit correspondant affiche désormais un message clair ("Aucun produit ne correspond à ce code-barres") au lieu de déclencher le bip de succès sans rien ajouter au panier.

14. **Suppression Multiple par Cases à Cocher & Export PDF/Excel (Nouveau)**
    - **Composant partagé `SelectionExportToolbar`** (`ui/components`) réutilisé sur les écrans Stock, Historique (Ventes + Réappro), Dettes et Mouvements de caisse : bascule un mode sélection avec case à cocher sur chaque carte pour la suppression groupée (avec confirmation), et deux boutons d'export (PDF / Excel) de la liste actuellement filtrée/affichée. Les actions individuelles (édition, suppression, remboursement, dépliage) sont masquées pendant le mode sélection pour éviter les clics accidentels.
    - **`ExportUtil`** : générateur générique de tableaux PDF paginés (Canvas/PdfDocument, même approche que les étiquettes code-barres) et de CSV UTF-8 avec BOM (ouverture directe dans Excel/Sheets), avec sauvegarde locale + copie dans `Downloads/EpicerieExports` et partage via le partage système Android (`FileProvider`, déclaré dans le manifeste). Exports détaillés dédiés : `exportSales()` (une ligne par article vendu), `exportStock()`, `exportDebts()`, `exportCaisseMouvements()`.

15. **Sauvegarde Cloud Firebase (Mis à jour)**
    - **`FirebaseBackupManager`** : client REST léger (OkHttp, déjà une dépendance du projet) pour l'API Firebase Realtime Database — aucune dépendance au SDK Firebase ni au plugin Gradle `google-services`, donc la compilation ne peut jamais être cassée par une configuration Firebase absente ou invalide. Realtime Database (et non Cloud Storage, devenu payant/Blaze fin 2024) a été choisi pour rester utilisable sur le plan gratuit Spark.
    - Section "Sauvegarde Cloud (Firebase)" dans Paramètres, sous la sauvegarde locale existante : l'URL Firebase Realtime Database se configure directement dans l'écran (`AppPreferences.firebaseDatabaseUrl`), sans rebuild nécessaire. Réutilise `getFullDatabaseJsonSync()`/`syncFullDatabaseSync()` déjà en place, donc le format de sauvegarde Cloud reste strictement identique à la sauvegarde locale et à la synchronisation réseau.
    - **Chemin de sauvegarde protégé par un jeton privé (Sécurité) :** Le chemin `backups/{token}` dans Realtime Database utilise `AppPreferences.firebaseBackupToken`, un UUID généré une seule fois et jamais affiché à l'écran — volontairement distinct de `installationId` (nombre à 6 chiffres affiché et copiable sur l'écran d'activation, donc devinable en ~10^6 essais). Avec des règles Realtime Database ouvertes (`.read`/`.write: true`, la configuration la plus simple pour un gérant non technique), c'est ce jeton à haute entropie qui protège concrètement l'accès aux données de chaque commerce.

16. **Impression du Reçu / Ticket de Caisse (Nouveau)**
    - **`ReceiptUtil`** génère un reçu PDF au format ticket de caisse (~80mm) après une vente validée depuis le bouton principal de la Caisse (`calculator_checkout_button`) : nom de l'épicerie, date/heure, détail des articles, total, espèces reçues, monnaie rendue. Une boîte de dialogue post-vente propose d'imprimer (réutilise `BarcodeUtil.printBarcode()`, générique malgré son nom) ou de partager le reçu.

17. **Écran Rapports & Tableau de bord (`DashboardScreen`) (Nouveau)**
    - Accessible depuis Paramètres : sélecteur de période (7 jours / 30 jours / ce mois-ci), cartes de synthèse (chiffre d'affaires, bénéfice net, taux de marge — même formule COGS que le "Kajy momba ny Vola" de l'Historique), graphique en barres du chiffre d'affaires par jour (Canvas Compose natif, aucune dépendance de graphique tierce), et liste des produits les plus vendus sur la période (triés par chiffre d'affaires, avec quantité vendue).

18. **Audit du Module de Synchronisation Multi-terminal (Nouveau)**
    - Vérification rigoureuse (même méthode que le correctif de duplication exponentielle, section 13) de tout le module P2P : `SyncManager.kt` ne fait aucune lecture directe de StateFlow (délègue entièrement à `InventoryViewModel` via l'interface `SyncBridge`), `reserveStockSync()`/`commitSaleSync()` interrogent le `repository` directement via `runBlocking` (jamais de `.value` figé), et `SyncService.kt` n'a aucun accès aux données (simple wrapper de service au premier plan). Conclusion : le module de synchronisation est confirmé exempt de ce type de bug — aucun correctif nécessaire.

19. **Modes de Paiement Réels à la Caisse (Nouveau)**
    - Le champ `Vente.modePaiement` (jusqu'ici toujours codé en dur sur `"ESPECES"`) reflète désormais le choix réel du vendeur. Sélecteur compact (`PaymentModeRow`, `CalculatorScreen`) proposant Espèces / Mvola / Orange Money sur les 3 écrans de caisse (téléphone, tablette, Calcul Rapide), masqué en mode Trosa puisqu'une vente à crédit est toujours enregistrée comme `"CREDIT"`. Le mode choisi est transmis à `InventoryRepository.checkoutSale()`, imprimé sur le reçu (`ReceiptUtil`), et propagé fidèlement à travers la synchronisation P2P (`commitSaleSync` lit désormais `modePaiement` depuis le JSON de vente au lieu de le recréer en dur côté serveur).

20. **Taxes / TVA Appliquées aux Calculs de Prix (Nouveau)**
    - Les champs `Product.taxable`/`tauxTaxe` (saisis dans `AddProductScreen` mais jamais utilisés) sont désormais exploités de bout en bout. Principe retenu : le prix de vente reste toujours **TTC** (le total payé par le client ne change jamais) — `taxable`/`tauxTaxe` servent uniquement à isoler la part de TVA déjà comprise dans ce prix, pour la traçabilité comptable. `CartItem` et `SoldItem` portent désormais ces deux champs (calcul `taxAmount` dérivé, aucune migration Room nécessaire côté `SoldItem` car il est sérialisé en JSON via `Converters`/Moshi) : le reçu imprime une ligne "Dont TVA" quand applicable, et le Tableau de bord (`DashboardScreen`) affiche une carte "TVA collectée" agrégée sur la période sélectionnée.

21. **Optimisation des Mises en Page pour Tablette / Grands Écrans (Nouveau)**
    - Audit ciblé des écrans sans aucune adaptation tablette (`AddProductScreen`, `SettingsScreen`, `DashboardScreen` n'avaient aucune référence à `screenWidthDp`/`isTablet`, contrairement à `HomeScreen`, `InventoryListScreen` et `CalculatorScreen` qui en avaient déjà). Correctif appliqué sans toucher au design existant : sur écran ≥ 600dp, le contenu de ces 3 écrans est désormais centré horizontalement avec une largeur maximale confortable (640–720dp) au lieu de s'étirer bord à bord ; sur téléphone, le comportement est strictement inchangé (`Modifier.fillMaxWidth()`, alignement `TopStart`). Ce traitement est à reproduire systématiquement sur tout nouvel écran ou écran retouché lors des prochaines fonctionnalités (C.2 Ouverture/fermeture de caisse, C.3 Rappels de dettes, etc.).

22. **Rappels de Dettes / Échéances (`Debt.dueDate`) (Nouveau)**
    - Champ optionnel `Debt.dueDate` (échéance) ajouté à l'entité Room `Debt` (migration destructive vers la version 16, protégée comme toujours par la sauvegarde de sécurité locale + Cloud Firebase qui restaurent automatiquement les données au démarrage suivant). Sélecteur de date Material3 (`DatePickerDialog`) dans le formulaire "Nouvelle dette" de `DebtsScreen`, entièrement optionnel.
    - Badge rouge "EN RETARD" / "TARA" sur la carte de dette (`DebtCard`) et dans l'export PDF/Excel (`ExportUtil.exportDebts`) dès que `dueDate` est dépassée et la dette non soldée (`Debt.isOverdue()`).
    - **Notification de rappel :** au démarrage de l'application, `InventoryViewModel` vérifie une fois par jour (calendrier, throttlé via `AppPreferences.lastOverdueDebtCheckDate`) s'il existe des dettes en retard et déclenche une notification groupée dédiée (`NotificationHelper.showOverdueDebtsNotification`, canal Android séparé `overdue_debt_alerts`) indiquant le nombre de dettes concernées et le montant total dû.
    - `dueDate` est propagé de bout en bout dans la sauvegarde de sécurité locale, la sauvegarde Cloud Firebase et la synchronisation P2P (`getFullDatabaseJsonSync`/`syncFullDatabaseSync`), au même titre que les autres champs des dettes.

23. **Ouverture / Fermeture de Caisse — Session de Travail (`CaisseSession`) (Nouveau)**
    - Nouvelle entité Room `CaisseSession` (migration destructive vers la version 17, protégée par les sauvegardes automatiques) : `dateOuverture`, `montantOuverture` (fond de caisse de départ), puis à la fermeture `dateFermeture`, `montantCompteFermeture` (espèces réellement comptées), `montantTheoriqueFermeture` (calculé) et `ecart` (différence).
    - **Carte "Session de caisse"** en haut de `CaisseMouvementsScreen` : bouton "Ouvrir la caisse" (saisie du fond de départ) quand aucune session n'est active, bouton "Fermer la caisse" (saisie du comptage) quand une session est ouverte, et récapitulatif de l'écart de la dernière session fermée (vert si négligeable, orange en excédent, rouge en manquant).
    - **Calcul théorique fiable :** au moment de la fermeture, le montant théorique = fond de départ + ventes en ESPECES uniquement (nouvelle requête `VenteDao.getCashSalesTotal`) + entrées manuelles − sorties manuelles, **strictement limité à la fenêtre temporelle de la session** (entre son ouverture et sa fermeture), pour ne pas mélanger plusieurs journées de travail.
    - **Correctif de cohérence lié à B.1 (modes de paiement réels) :** `InventoryViewModel.soldeCaisse` (solde de caisse théorique global affiché en haut de l'écran) supposait auparavant que "toutes les ventes sont en espèces" — devenu faux dès que les modes Mvola/Orange Money existent réellement. Corrigé pour ne sommer que les ventes `modePaiement == "ESPECES"` (via la nouvelle relation `allVentes`), sinon l'ajout des modes de paiement aurait silencieusement gonflé le solde de caisse affiché avec des ventes qui ne sont jamais passées par le tiroir-caisse.
    - `CaisseSession` est propagée dans la sauvegarde locale, Firebase et la synchronisation P2P (`SyncSerializer.serializeFullSync`/`deserializeFullSync` étendus avec un champ `caisseSessions` rétrocompatible, valeur par défaut `"[]"`), avec fusion intelligente : si une session a été fermée sur un autre terminal, sa fermeture (comptage, écart) est propagée aux autres appareils au lieu d'être dupliquée.

24. **Sauvegarde Firebase Automatique dès qu'il y a Internet (Nouveau)**
    - Nouvel utilitaire `NetworkMonitor` (`ConnectivityManager`) : `isOnline(context)` pour une vérification ponctuelle, `observeOnline(context, onAvailable)` pour un callback déclenché à chaque fois que l'appareil retrouve une connexion internet valide.
    - `InventoryViewModel.triggerLocalSafetyBackup()` (déjà appelé après chaque mutation) déclenche désormais aussi `maybeAutoBackupToFirebase()` : si une URL Firebase est configurée dans les Paramètres **et** que l'appareil est en ligne à cet instant, la sauvegarde JSON complète est envoyée automatiquement vers Firebase Realtime Database — sans action manuelle de l'utilisateur.
    - Un callback de connectivité est aussi enregistré dès le démarrage (`init`, nettoyé dans `onCleared()`) pour pousser immédiatement la sauvegarde dès que la connexion revient, plutôt que d'attendre la prochaine transaction (couvre le cas où l'utilisateur a travaillé hors-ligne puis retrouve internet sans rien modifier tout de suite après).
    - Le bouton manuel "Envoyer" dans Paramètres reste disponible ; un message vert confirme que la sauvegarde est désormais automatique dès qu'une URL est configurée.

25. **Activation du Flash / Torche lors du Scan de Codes-Barres (Nouveau)**
    - `BarcodeScannerView` (composant partagé utilisé par la Caisse et l'ajout de produit) expose désormais un bouton flash dans son en-tête, visible uniquement quand l'appareil possède une unité flash (`CameraInfo.hasFlashUnit()`), utilisant l'API CameraX standard `CameraControl.enableTorch(Boolean)`. L'état visuel (icône pleine/vide, fond coloré) reflète l'état réel de la torche ; l'état est ré-appliqué automatiquement si la caméra est re-liée (rotation d'écran, etc.).

26. **Remplacement du Bouton de Synchronisation de la Barre Supérieure (Nouveau)**
    - L'icône de synchronisation en haut de l'écran d'accueil (à côté des Paramètres) ouvrait auparavant un dialogue d'exclusion sélective de produits de la synchronisation — une fonctionnalité de configuration avancée peu intuitive à cet emplacement. Elle a été retirée de ce bouton et remplacée par une action directe et utile : si l'appareil est déjà connecté à un autre terminal (mode Serveur/Client actif), un clic déclenche immédiatement `SyncManager.triggerDatabaseSync()` (avec confirmation visuelle) ; sinon, il ouvre directement l'écran de Synchronisation pour établir une connexion. Un petit point vert apparaît sur l'icône quand une connexion P2P est active.

27. **Comptes Utilisateurs & Rôles — PIN Vendeur / Gérant (Nouveau, B.3/E.2)**
    - Nouvelle entité Room `Vendeur` (migration destructive vers la version 18, protégée comme toujours par les sauvegardes automatiques) : `nom`, `pinHash` (SHA-256 du PIN, jamais le PIN en clair), `role` (`GERANT`/`VENDEUR`), `actif`. **Fonctionnalité 100% optionnelle** : tant qu'aucun compte n'est créé, l'application se comporte exactement comme avant — aucune connexion, aucun PIN, aucune restriction nulle part.
    - **Gestion des comptes :** nouvelle carte "Comptes & Rôles" dans Paramètres (ajout/édition/suppression, choix du rôle, activation/désactivation, changement de PIN).
    - **Sélecteur de vendeur actif :** dès qu'au moins un compte existe, une icône apparaît dans la barre supérieure (à côté de la synchronisation) affichant l'initiale du vendeur connecté ; un clic permet de choisir son nom dans la liste puis de saisir son PIN pour "prendre" la caisse, ou de se déconnecter. Le vendeur actif est mémorisé entre les lancements de l'application (`AppPreferences.activeVendeurId`).
    - **`Vente.vendeurId` enfin renseigné :** ce champ existait dans le schéma relationnel depuis longtemps sans jamais être rempli — chaque vente enregistre désormais l'id du vendeur actif au moment du paiement (`InventoryViewModel.checkoutCart` → `InventoryRepository.checkoutSale`), permettant une vraie traçabilité des ventes par employé.
    - **Paramètres protégés par le PIN Gérant :** dès qu'au moins un compte existe et qu'aucun gérant n'est actuellement connecté sur l'appareil, l'accès à Paramètres (depuis n'importe quel point d'entrée : barre supérieure, navigation basse, tiroir tablette) déclenche une invite de PIN gérant au lieu d'ouvrir directement l'écran — protégeant ainsi la configuration Firebase, les sauvegardes et la gestion des comptes eux-mêmes contre un simple vendeur.
    - `Vendeur` (y compris le hash du PIN, jamais le PIN en clair) est propagé dans la sauvegarde locale, Firebase et la synchronisation P2P au même titre que les autres entités (fusion par nom, sans duplication), afin que les comptes créés sur un terminal soient utilisables sur tous les autres.

28. **Retours / Remboursements (Nouveau, C.1)**
    - Nouvelle entité Room `Retour` (migration destructive vers la version 19, protégée par les sauvegardes automatiques) : `saleId`, `items` (liste d'articles retournés, quantités pouvant être inférieures à la vente d'origine pour un retour partiel), `totalAmount`, `motif`, `modePaiementOrigine`. La vente d'origine n'est jamais modifiée — le retour est un enregistrement séparé, pour garder une traçabilité comptable complète.
    - **Bouton "Retour" dans l'Historique des ventes :** disponible sur chaque vente dépliée tant qu'il reste des articles retournables. Le dialogue affiche chaque article avec la quantité maximale retournable (déjà décomptée des retours précédents) et un champ de quantité éditable, plus un motif optionnel — permet un retour total ou partiel, article par article.
    - **Restauration réelle du stock :** `InventoryRepository.processReturn()` ré-incrémente `Product.stock` et `Produit.quantiteStock` (avec entrée `MouvementStock` de type `"RETOUR"` pour la traçabilité), exactement à l'inverse de `checkoutSale()`.
    - **Impact correct sur la caisse :** si la vente d'origine était en `ESPECES` (déterminé en retrouvant la `Vente` correspondante par date/montant, puisque le modèle `Sale` historique ne porte pas `modePaiement`), le retour crée automatiquement une `MouvementCaisse` de type `SORTIE` — un remboursement en espèces fait sortir de l'argent du tiroir-caisse. Les remboursements Mvola/Orange Money/Crédit n'y touchent pas.
    - **Rapports nets des retours :** `DashboardScreen` déduit désormais les retours (montant et coût des marchandises) du chiffre d'affaires et du bénéfice net de la période, plus une carte "Retours/remboursements" dédiée ; un badge "Retourné"/"Retour partiel" apparaît sur la vente concernée dans l'Historique.
    - Correctif connexe : la sauvegarde complète (`getFullDatabaseJsonSync`/`syncFullDatabaseSync`) ne portait pas les champs `taxable`/`tauxTaxe` des articles vendus (seule la synchronisation P2P en checkout les avait) — corrigé au passage pour que la ventilation TVA (section 20) survive à une restauration de sauvegarde.
    - `Retour` est propagé dans la sauvegarde locale, Firebase et la synchronisation P2P en ajout pur (aucune donnée déjà synchronisée — produits, mouvements de caisse — n'est ré-appliquée deux fois, ces effets étant déjà couverts par leurs propres fusions dans le même cycle de synchronisation).
    - **Limite connue :** une vente à crédit (Trosa) retournée ne touche pas automatiquement la dette associée — le gérant doit l'ajuster manuellement dans l'écran Dettes si nécessaire.

29. **Alertes de Péremption (Nouveau, C.4)**
    - Le schéma relationnel avait déjà `Product.gerePeremption` (case à cocher dans la fiche produit) et l'entité `LotProduit` (`lots_produit`) depuis le début du projet, mais sans aucun écran ni notification pour les exploiter — corrigé. Aucune migration de base de données nécessaire, la table existait déjà.
    - **Nouvel écran `PeremptionScreen`** (accessible depuis une carte dédiée dans Paramètres) : carte de synthèse (nombre de lots périmés / bientôt périmés sous 7 jours), liste de tous les lots triée par date de péremption la plus proche, avec code couleur (rouge = périmé, orange = à surveiller, vert = ok). Ajout/édition/suppression d'un lot (produit, numéro de lot optionnel, quantité, date de péremption via sélecteur Material3), limité aux produits ayant `gerePeremption` activé.
    - **Notification quotidienne** (canal Android dédié `expiry_alerts`, throttlée à une fois par jour comme les rappels de dettes) dès qu'au moins un lot est périmé ou expire dans les 7 jours.
    - **Propagation prudente entre appareils :** `LotProduit.produitId` référence la table relationnelle `Produit`, dont l'id local peut différer d'un appareil à l'autre après une fusion de synchronisation (un produit "nouveau" reçoit un id auto-généré localement). Pour éviter qu'un lot ne s'attache au mauvais produit sur un autre terminal, la sauvegarde/synchronisation identifie chaque lot par le code-barres ou le nom du produit plutôt que par son id brut, et résout l'id local correspondant à la réception — exactement comme la fusion des produits eux-mêmes.

30. **Suppression Propagée à la Sauvegarde Cloud et à la Synchronisation — Tombstones (Correctif de fiabilité)**
    - **Constat vérifié à la demande de l'utilisateur :** chaque fonction de suppression (`deleteProduct`, `deleteSale`, `deleteDebt`, `deleteRestock`, `deleteMouvementCaisse`, `deleteVendeur`, `deleteRetour`, `deleteLot`) appelait déjà `triggerLocalSafetyBackup()`, qui déclenche depuis la section 24 un ré-envoi automatique vers Firebase — donc une suppression locale était déjà répercutée sur Firebase immédiatement (ou dès le retour de connexion si hors-ligne). Le vrai trou de fiabilité identifié était ailleurs : `syncFullDatabaseSync()` (utilisée pour la restauration de sauvegarde locale au démarrage, la restauration manuelle depuis Firebase, **et** la synchronisation P2P entre appareils) est purement additive par conception — elle ne supprime jamais rien localement. Un produit ou une vente supprimés pouvaient donc réapparaître si une sauvegarde ou un appareil pair contenant encore l'ancienne donnée était restauré/synchronisé après coup.
    - **Nouvelle entité Room `DeletedRecord`** (`deleted_records`, migration destructive vers la version 20) : une ligne "tombstone" par suppression, avec un `entityType` (`product`, `sale`, `debt`, `restock`, `mouvementCaisse`, `vendeur`, `retour`, `lot`) et une `naturalKey` **stable entre appareils** (jamais l'id local auto-incrémenté, qui peut différer d'un terminal à l'autre pour le même enregistrement réel) — code-barres/SKU/nom pour un produit, horodatage pour une vente, nom+date pour une dette, etc. (voir `TombstoneKeys` dans `InventoryViewModel`), cohérente avec les clés de correspondance déjà utilisées par chaque fusion.
    - Chacune des 8 fonctions de suppression enregistre désormais un tombstone en plus de la suppression elle-même.
    - `getFullDatabaseJsonSync()`/`syncFullDatabaseSync()`/`SyncSerializer` propagent la liste des tombstones au même titre que les autres données. À la fusion, **avant** de traiter chaque type d'entité : (1) les nouveaux tombstones reçus sont enregistrés localement, (2) tout enregistrement local correspondant à un tombstone nouvellement appris est supprimé (propage réellement la suppression faite sur un autre appareil), (3) chaque insertion "nouvel enregistrement" est bloquée si sa clé naturelle est tombstonée (empêche la résurrection lors d'une restauration ou d'une synchronisation avec un pair qui ne connaît pas encore la suppression).
    - Une suppression volontaire (via l'écran) reste distincte d'une re-création manuelle ultérieure du même produit/code-barres : le tombstone ne bloque que la résurrection automatique via fusion de synchronisation, jamais un ajout normal depuis l'interface.

31. **Fusion des Dettes par Débiteur (`DebtorGroup`, `DebtorGroupCard`) (Nouveau)**
    - **Demande utilisateur :** quand un même client (nom identique, majuscule/minuscule confondues) a plusieurs dettes distinctes (ex. plusieurs achats à crédit "Trosa" à des dates différentes), l'écran Dettes doit les fusionner visuellement et afficher le total dû, tout en gardant le détail de chaque date/montant consultable.
    - `DebtsScreen` regroupe désormais la liste `debts` par `debtorName.trim().lowercase()` (correspondance exacte du nom complet normalisé, volontairement **pas** un préfixe, pour ne jamais fusionner deux personnes différentes par erreur) via une nouvelle `data class DebtorGroup(displayName, debts)` avec les propriétés calculées `totalBalance`, `totalAmount`, `isFullyPaid`, `hasOverdue`.
    - Important : **aucune donnée n'est fusionnée en base** — chaque ligne `Debt` (sa propre date, son propre montant, sa propre note) reste un enregistrement Room séparé et indépendant. Le regroupement n'existe qu'au niveau de l'affichage (`LazyColumn` de `DebtsScreen`).
    - Un débiteur avec une seule dette continue d'afficher l'unique `DebtCard` existante, strictement inchangée (aucun changement visuel pour le cas courant). Un débiteur avec plusieurs dettes affiche la nouvelle `DebtorGroupCard` : un en-tête consolidé (nom, solde total, badge payé/non-payé, badge retard si au moins une dette est en retard) que l'on peut déplier pour révéler chaque `DebtCard` individuelle du groupe (avec sa propre date, son propre solde, ses propres actions rembourser/supprimer).
    - L'export (`ExportUtil.exportDebts`) et la recherche/filtrage (`viewModel.filteredDebts`, `viewModel.debtFilter`) continuent d'opérer sur la liste `debts` à plat, transaction par transaction — non affectés par ce regroupement purement visuel.


32. **Cohérence du Prix Affiché entre l'Écran d'Accueil et l'Écran Stock (`PriceUtil`) (Correctif)**
    - **Constat utilisateur :** la carte d'un même produit n'affichait pas le même prix sur l'écran d'accueil et sur l'écran Stock, l'écart apparaissant après un réapprovisionnement.
    - **Deux causes réelles identifiées.** (1) Les deux écrans lisaient bien la même donnée (`viewModel.filteredProducts`) mais appliquaient des règles différentes : `HomeScreen` honorait le mode de vente « Gros » (affichage de `wholesalePrice`), `InventoryListScreen` affichait toujours `product.price`. (2) `CommissionScreen` (réapprovisionnement) écrasait `wholesalePrice` — qui est un prix de **vente** en gros saisi dans la fiche produit — avec le **coût d'achat** unitaire du réappro, ce qui faisait à la fois vendre à prix coûtant en mode Grossiste et diverger les deux écrans à chaque approvisionnement.
    - **Nouveau `util/PriceUtil`** : `isWholesaleActive(product, shopMode)` et `displayPrice(product, shopMode)` sont désormais la seule règle de calcul du prix affiché sur une carte produit. `HomeScreen` et `InventoryListScreen` passent tous deux par elle (badge « Gros » compris), donc ils ne peuvent plus diverger par construction.
    - Le réappro n'écrit plus que dans `prixAchatUniteBase` pour le coût d'achat ; `wholesalePrice` reste sous le contrôle exclusif de la fiche produit.

33. **Détail des Articles d'un Trosa (`DebtDetailDialog`) (Nouveau)**
    - **Demande utilisateur :** la carte d'une dette tronque la liste des articles avec des « … » quand un nom d'article est long, rendant le détail illisible.
    - Les articles d'une vente à crédit sont stockés dans `Debt.note` sous la forme `"Article (2 x 1 500), Autre (1 x 3 000)"` (généré par `CalculatorScreen` au moment du checkout « Trosa »).
    - Un appui sur une carte de dette (hors mode sélection) ouvre `DebtDetailDialog` : liste numérotée des articles **sans troncature**, plus le récapitulatif montant initial / déjà payé / reste à payer / échéance, et un bouton « Rembourser » direct. Le découpage se fait sur les virgules suivant une parenthèse fermante, ce qui préserve les noms d'articles contenant eux-mêmes une virgule ; une note saisie à la main (format libre) est affichée telle quelle.

34. **Identification par Appareil sur Chaque Transaction (Nouveau)**
    - **Contexte :** plusieurs personnes (gérant + employés) utilisent l'app sur des appareils différents partageant les mêmes données. Chaque transaction doit pouvoir être rattachée au poste qui l'a enregistrée.
    - `AppPreferences.deviceName` : nom lisible de **l'appareil** (« Téléphone de Koto », « Caisse 2 »), volontairement distinct de `groceryName` (nom de l'épicerie). Par défaut le modèle du téléphone, renommable depuis l'écran Épicerie.
    - Les entités `Sale`, `Debt`, `Restock`, `MouvementCaisse` et `Retour` portent désormais `deviceName` et `vendeurNom`, renseignés à l'enregistrement. Une vente arrivant d'un terminal client via la synchronisation P2P conserve **le nom du terminal d'origine**, pas celui du serveur qui la valide.
    - Ces champs sont propagés dans la sauvegarde locale/Firebase et la synchronisation (`getFullDatabaseJsonSync`/`syncFullDatabaseSync`), et relus avec `optString(..., "")` : les sauvegardes antérieures restent restaurables, sans auteur connu.
    - La sortie de caisse générée automatiquement par un remboursement porte la même signature que le retour qui l'a provoquée.

35. **Droits Employés : Suppression d'Historique Réservée au Gérant (Nouveau)**
    - `InventoryViewModel.estGerant` / `peutSupprimerHistorique()` : tant qu'**aucun** compte employé n'existe (cas de l'épicier seul), tout reste autorisé et rien ne change ; dès que des comptes existent, seul un gérant connecté peut supprimer.
    - Gestes protégés : `deleteSale`, `deleteDebt`, `deleteRestock`, `deleteMouvementCaisse`, `deleteRetour`, `deleteProduct`, `purgerJournalAudit`. Ces fonctions retournent désormais un `Boolean` ; un refus affiche un message (observateur unique dans `MainAppLayout`, donc valable sur tous les écrans) et enregistre une trace « tentative bloquée ».
    - Les suppressions propagées par la synchronisation passent par le dépôt et ne sont pas concernées par ce contrôle (ce n'est pas un geste utilisateur).

36. **Journal d'Audit et Détection d'Anomalies / Triche (`AuditLog`, `AnomalyDetector`, `SecuriteScreen`) (Nouveau)**
    - **Nouvelle entité Room `AuditLog`** (`audit_logs`) : trace horodatée de chaque geste sensible avec l'appareil, l'employé, le rôle, la cible, le montant, une sévérité (INFO / ATTENTION / ALERTE) et un indicateur « action refusée ».
    - Alimenté par : suppressions (vente, dette, réappro, mouvement de caisse, retour, produit), tentatives refusées, retours, sorties de caisse, écarts (manquants) à la fermeture de caisse, ajustements de stock **à la baisse**, connexions vendeur, purge du journal.
    - **`util/AnomalyDetector`** — fonction pure (données → alertes), sans dépendance Android, donc testable : manquants de caisse isolés et récurrents, suppressions d'historique, tentatives bloquées, retours fréquents par poste, ventes en dessous du prix d'achat, sorties de caisse élevées, ventes hors horaires (22h–5h). Chaque anomalie porte un titre, une description en langue active, le responsable, le montant et le nombre d'occurrences.
    - **Nouvel écran `SecuriteScreen`** (carte dédiée dans Paramètres) : onglet « Alertes » (anomalies triées par gravité) et onglet « Journal » (traces brutes), avec fenêtre d'analyse 7 / 30 / 90 jours. L'écran ne bloque rien : il rend visible ce qui, autrement, ne se voit qu'au moment où l'argent manque.
    - **Migration Room 20 → 21 écrite explicitement** (`MIGRATION_20_21`) au lieu de laisser `fallbackToDestructiveMigration` opérer : une épicerie déjà en production aurait sinon perdu tout son historique à l'installation de cette mise à jour.

37. **Fiche d'Identité de l'Épicerie et Logo dans Tous les PDF (`EpicerieScreen`, `ShopInfo`, `ShopLogoUtil`) (Nouveau)**
    - **Nouvel écran `EpicerieScreen`** regroupant tout ce qui décrit le commerce : nom, logo importable depuis la galerie, adresse, téléphone, NIF/STAT, message de pied de reçu — plus, dans une section séparée, le nom de **cet** appareil (qui décrit le poste, pas l'épicerie).
    - Le champ « nom de l'épicerie » isolé dans Paramètres est remplacé par une carte de redirection vers cet écran : une seule saisie, et l'écran Paramètres s'allège.
    - `ShopLogoUtil` recopie le logo choisi dans le stockage privé de l'app et le redimensionne à 512 px max : une URI de galerie n'est pas lisible durablement (permission révoquée, fichier déplacé) alors qu'un reçu doit rester réimprimable des mois plus tard.
    - `ShopInfo.depuis(context)` lit la fiche depuis les préférences **au moment de générer un PDF**. `ReceiptUtil` (reçus de caisse) et `ExportUtil.exportToPdf` (rapports : ventes, stock, dettes, mouvements de caisse, réappros) l'utilisent directement : le logo et les coordonnées apparaissent donc automatiquement sur **tous** les documents, sans modifier les ~15 sites d'appel existants.

38. **Licence et Activation après Paiement sur Base Firebase Centrale (`LicenceManager`) (Nouveau)**
    - **Décision d'architecture :** une seule base Realtime Database, celle du **développeur**, renseignée en dur dans `LicenceManager.CENTRAL_DATABASE_URL`. Le client n'a plus rien à créer ni à configurer (l'ancien parcours « créez votre propre projet Firebase » demandait une compétence que l'épicier n'a pas).
    - Parcours : l'app démarre verrouillée et affiche son ID d'installation à 6 chiffres → le client paie et communique cet ID → le développeur crée `/licences/<id>` dans la console Firebase (`actif`, `expiration`, `epicerie`, `note`) → le client appuie sur « Vérifier mon activation » et l'app se déverrouille.
    - **Règles de sécurité à poser sur cette base** (documentées dans `LicenceManager`) : `licences` en lecture seule pour les clients (`".write": false`), sinon n'importe qui pourrait s'auto-activer ; `backups` reste lisible/écrivable par chemin secret (`firebaseBackupToken`).
    - Une licence **explicitement** suspendue ou expirée reverrouille l'app à la vérification suivante (contrôle discret au démarrage, au plus une fois tous les 3 jours). Une licence simplement *introuvable* ne reverrouille pas une app déjà activée : une épicerie activée hors-ligne par code à 6 chiffres n'a pas forcément de nœud créé côté Firebase et se retrouverait bloquée en pleine journée de vente pour une raison purement administrative. Une panne réseau ne verrouille **jamais** une épicerie en train de travailler : tolérance hors-ligne de 30 jours après la dernière vérification réussie (`estUtilisableHorsLigne`).
    - Le code d'activation manuel à 6 chiffres est conservé comme chemin de secours pour une installation sans réseau.
    - **Limite volontaire :** le verrouillage protège contre l'usage non payé ordinaire, pas contre un utilisateur qui modifierait l'APK ou les préférences d'un appareil rooté.

39. **Deux Bases Firebase Distinctes : Licences et Sauvegardes (Nouveau)**
    - **Décision d'architecture :** les licences (`LicenceManager.CENTRAL_DATABASE_URL`) et les sauvegardes clients (`FirebaseBackupManager.CENTRAL_BACKUP_DATABASE_URL`) vivent dans **deux projets Firebase séparés**, tous deux appartenant au développeur.
    - Pourquoi séparer : (1) les sauvegardes sont ce qui remplit le quota (1 Go sur le plan gratuit) alors qu'une licence pèse quelques octets — dans une base commune, une saturation des sauvegardes empêcherait aussi les **nouveaux clients de s'activer**, ce qui transformerait un problème de stockage en panne commerciale ; (2) les règles de sécurité sont opposées (licences en lecture seule côté client, sauvegardes écrivables) ; (3) on peut purger, migrer ou facturer les sauvegardes sans jamais toucher au verrouillage.
    - `FirebaseBackupManager.resolveDatabaseUrl(userUrl)` : l'URL saisie par le gérant dans Paramètres l'emporte si elle existe, sinon on retombe sur la base du développeur. La sauvegarde cloud fonctionne donc **dès l'installation**, sans que le client crée le moindre projet Firebase — l'ancien parcours « créez votre propre base » restait une barrière que l'épicier ne franchissait pas.
    - Le champ URL de Paramètres devient explicitement facultatif (libellé « URL Firebase personnelle (facultatif) ») et n'est plus bloquant : les boutons Sauvegarder/Restaurer fonctionnent champ vide. Il ne sert plus qu'au gérant qui veut héberger ses données lui-même.
    - **Règles de sécurité de la base des sauvegardes** : l'autorisation est donnée sous `backups/$token`, **jamais** sur `backups` lui-même — sinon un seul `GET /backups.json` récupérerait les données de tous les clients d'un coup. Le token est un UUID aléatoire jamais affiché à l'écran (`AppPreferences.firebaseBackupToken`).
    - **Limite connue :** les règles ouvertes par chemin secret protègent contre la lecture opportuniste, pas contre un client qui connaîtrait le token d'un autre. Passer à Firebase Auth (anonyme) + règles par UID est le prochain palier si le volume ou la sensibilité l'exigent.

40. **Sauvegardes Sans Photos, Regroupées, et Code de Récupération (F1 — Correctif majeur)**
    - **Trois défauts cumulés, dont un qui vidait la fonctionnalité de son sens.**
    - (1) *Les photos voyageaient dans chaque sauvegarde.* `getFullDatabaseJsonSync()` encodait toutes les images produits en base64 (JPEG 1024 px qualité 82, ~150 Ko pièce, +33 % en base64). Une épicerie de 60 produits photographiés produisait ~12 Mo de JSON. Le paramètre `includeImages` (défaut **false**) les exclut désormais des sauvegardes locale et cloud ; seule la synchronisation P2P le passe à **true**, car elle transite par le Wi-Fi local (gratuit) et l'appareil pair n'a aucun autre moyen d'obtenir les photos.
    - (2) *Ce JSON était régénéré à chaque mutation.* `triggerLocalSafetyBackup()` a une vingtaine de points d'appel : un panier de 5 articles déclenchait autant de sérialisations complètes, d'écritures disque et de téléversements. Les envois sont maintenant **regroupés** — un seul travail en vol, relancé à chaque sollicitation : 3 s pour le disque, 60 s pour le réseau (payant au mégaoctet pour l'épicier). `flushBackupsNow()` force un envoi immédiat sur `ON_STOP`, le seul instant où l'app risque d'être tuée avant la fin du délai.
    - (3) *La sauvegarde était irrécupérable.* Elle était rangée sous `backups/<UUID aléatoire>` stocké uniquement dans les préférences du téléphone et **jamais affiché**. Téléphone perdu → nouvelle installation → nouvel UUID → chemin vide : la sauvegarde existait toujours sur Firebase mais plus personne n'en connaissait l'adresse, alors que l'écran Paramètres promettait une protection « même en cas de perte du téléphone ».
    - **Nouveau `util/RecoveryCode`** : code de 12 caractères en Crockford base32 (ni I, ni L, ni O, ni U — les confusions de dictée), affiché sous la forme `VRT-A3F9-K2M7-QP4X`, soit 60 bits d'entropie. La normalisation tolère minuscules, espaces, tirets manquants et confusions O/0, I/1, L/1, U/V ; une saisie invalide est **refusée sans rien modifier**, car écraser le code sur une faute de frappe couperait l'accès à la sauvegarde en cours.
    - Paramètres affiche le code (avec bouton copier), la date du dernier envoi cloud réussi, et un bouton « Nouveau téléphone ? Saisir mon ancien code » qui rebranche l'appareil puis restaure. Le code est à noter par le gérant **et** à communiquer au fournisseur en même temps que l'ID d'installation : c'est ce double dépôt qui permet la récupération quand le client a tout perdu.
    - **Récupération des photos après restauration** : `rechercherPhotosManquantes()` redemande à Open Food Facts (déjà intégré, gratuit) l'image des produits qui ont un code-barres et dont le fichier local a disparu. Déclenchée après chaque restauration cloud. Les produits sans code-barres gardent leur icône jusqu'à ce que le gérant les photographie.
    - **Conséquence de capacité :** une sauvegarde passe de ~12 Mo à quelques dizaines de Ko, ce qui ramène le plan Firebase gratuit d'environ 80 boutiques à plusieurs dizaines de milliers.

41. **Fin des Migrations Destructives Silencieuses (F4 — Correctif)**
    - `fallbackToDestructiveMigration(true)` s'appliquait à **toutes** les versions : la première mise à jour livrée sans migration écrite aurait effacé l'historique de ventes, de dettes et de caisse de chaque épicerie en production, sans message ni récupération possible.
    - Remplacé par `fallbackToDestructiveMigrationFrom(true, 1..19)` : la destruction reste tolérée pour les versions d'avant la mise en production (de toute façon déjà effacées par ce mécanisme), mais à partir de la version 20 **toute évolution de schéma exige une migration**. À défaut, l'app échoue bruyamment dès le premier lancement de développement — un échec qui se remarque, plutôt qu'une perte de données qui ne se remarque que chez le client.

42. **Catalogue Complet au-delà de 100 Produits (F6 — Correctif)**
    - `getLimitedProducts(100)` et le `LIMIT 100` de `searchProducts` faisaient disparaître, **sans aucun message**, toute référence au-delà de la centième : une épicerie de 150 produits en perdait 50 à l'accueil, dans l'écran Stock et dans la recherche, alors qu'ils étaient bien en base et décomptés dans les ventes.
    - `allProducts` lit désormais le catalogue complet et la requête de recherche n'est plus bornée. Les listes étant paresseuses (`LazyColumn`/`LazyVerticalGrid`), seules les lignes visibles sont composées ; en mémoire un catalogue de 2000 références pèse ~1 Mo.

43. **Liaison Appareils ↔ Licence, Identité `ANDROID_ID`, État `TRANSFEREE` (Anti-« fausse perte »)**
    - **Problème visé :** un client pouvait prétendre avoir perdu son téléphone pour obtenir une seconde activation et équiper un autre appareil — rien ne liait la licence aux téléphones physiques, et rien ne désactivait jamais l'ancien.
    - **Nouveau `util/DeviceIdentity`** : identité stable du téléphone dérivée de `Settings.Secure.ANDROID_ID` (constant par couple appareil/clé de signature, il survit aux effacements de données de l'app, contrairement à l'ID d'installation qui vit dans les préférences). Forme canonique hex minuscule (clé Firebase), forme lisible `A3F2-9B10-C44D-E5F6` affichée sur l'écran d'activation. Repli sur un UUID persisté pour les rares appareils au `ANDROID_ID` absent ou bugué (`9774d56d682e549c`).
    - **Le nœud de licence** (`/licences/<n°>`, le n° = ID d'installation du premier appareil) porte désormais `appareils: { <codeAppareil>: {nom, depuis} }`, plus `maxAppareils`, `transferts` (compteur de « pertes » — un client qui perd trois téléphones dans l'année devient visible) et `recoveryCode` (copie du code de récupération des sauvegardes). La liste n'est modifiable **que par le fournisseur dans la console** (choix délibéré, règles en `.write: false`) : ajouter un poste ou transférer passe par lui.
    - `LicenceManager.verifierLicence(numeroLicence, deviceId)` : appareil hors liste → nouvel état **`TRANSFEREE`**, qui verrouille aussi au contrôle périodique — c'est ce verrouillage différé (3 jours au plus) qui rend un transfert effectif sur l'ancien téléphone sans intervention. Un nœud **sans** champ `appareils` reste accepté (licences d'avant cette version). Le nom de la boutique (`epicerie`) est annoncé au déverrouillage : une licence prêtée afficherait le nom du voisin.
    - **Écran d'activation** : affiche le code appareil sous l'ID (copie des deux en un geste), et un champ « N° de licence existante » pour rattacher un appareil supplémentaire ou un téléphone de remplacement (`AppPreferences.licenceNumber`, mémorisé après le premier succès).
    - **Tolérance hors-ligne enfin câblée** : `estUtilisableHorsLigne` (30 jours) n'était appelée nulle part. Elle pilote maintenant la cadence de re-vérification : tous les 3 jours dans la fenêtre, à chaque démarrage au-delà — sans jamais verrouiller sur une panne réseau.
    - **Limite documentée :** le contrôle est fait par l'app ; un APK recompilé le contourne. L'objectif est la fraude ordinaire (« bouche à oreille »), pas le rétro-ingénieur — une exécution côté serveur exigerait Cloud Functions (plan Blaze).

44. **Code d'Activation Hors-ligne en HMAC-SHA256 (F3 — Correctif de sécurité)**
    - L'ancienne formule `(id × 3 + 123456) mod 10^6` se calculait de tête à partir de l'ID affiché en clair sur l'écran de verrouillage : une fuite de la formule (un post Facebook) aurait activé gratuitement toutes les installations.
    - **Nouveau `util/OfflineActivationCode`** : 8 premiers chiffres d'un HMAC-SHA256 à secret embarqué. La connaissance de mille couples (ID, code) ne permet pas d'en déduire un autre. Le fournisseur calcule les codes avec une one-liner python documentée (dans le fichier et le guide de gestion) ; l'équivalence exacte Kotlin ↔ python (4 premiers octets big-endian = 8 premiers hex) est verrouillée par `OfflineActivationCodeTest` sur des vecteurs de référence.
    - Limite assumée dans le KDoc : le secret vit dans l'APK, extractible par rétro-ingénierie — plafond indépassable de toute activation hors-ligne ; la vérification en ligne reprend la main dès que l'appareil a du réseau.
    - Libellés mis à jour (8 chiffres), `RecoveryCodeTest` ajouté au passage pour figer la normalisation du code de récupération.

45. **Garanties de Non-Effacement des Données à la Mise à Jour (Audit + Tests)**
    - **Trois lignes de défense vérifiées pour le passage v20 -> v21 :** (1) la migration `MIGRATION_20_21` (noms de tables contrôlés contre les `@Entity` réels : sales, debts, restocks, mouvements_caisse, retours) ; (2) le filet de restauration au démarrage — `init` du ViewModel rejoue `restoreLocalSafetyBackup()` dès qu'un fichier de sauvegarde locale existe, celui-ci survivant à la mise à jour ; (3) la sauvegarde cloud sous le code de récupération, inchangé pour les clients existants (la clé de préférence `key_firebase_backup_token` est réutilisée telle quelle — un jeton UUID existant n'est jamais régénéré).
    - **Nouveau `Migration20To21Test`** (Robolectric) : construit une base réelle au schéma v20, la peuple (ventes, dette avec note, réappro, mouvement de caisse, retour), exécute la migration et vérifie que chaque ligne survit, que les nouvelles colonnes portent leur défaut, que `audit_logs` accepte une écriture et que les valeurs métier (nom du débiteur, solde, note) sont intactes.
    - **La CI exécute désormais les tests unitaires** (`testDebugUnitTest` ajouté à build_apk.yml avant l'assemblage) : casser la préservation des données fait échouer la CI, pas une boutique.
    - **Compatibilité des codes de récupération historiques** : les boutiques activées avant l'introduction du code lisible ont leur sauvegarde sous un UUID ; `RecoveryCode.normalizeLegacyUuid` (couvert par `RecoveryCodeTest`) reconstruit exactement la forme stockée (minuscules, tirets 8-4-4-4-12) pour que la re-saisie sur un nouveau téléphone retrouve l'ancien coffre. `applyRecoveryCode` accepte les deux générations.
    - **Vérifié à l'exécution (projet JVM isolé, 8 tests verts)** : équivalence HMAC Kotlin <-> commande python du fournisseur, aller-retour generate/format/normalize, normalisation des UUID historiques. Le reste (migration Room, Robolectric) s'exécute dans la CI GitHub, qui dispose du SDK Android absent de cet environnement.
    - Rappel des invariants de mise à jour : nom de base inchangé (`varotra_database_v4`), clés de préférences inchangées (`key_is_activated`, `key_firebase_backup_token`…), sauvegardes/synchro JSON rétro- et inter-compatibles (`optString` partout), destruction limitée aux versions 1 à 19 (déjà détruites par l'ancien mécanisme).

46. **Photos: Stockage Unifié, Archive ZIP de Sauvegarde, et Correction du Plantage à la Prise de Photo**
    - **Plantage corrigé (cause certaine) :** `AddProductScreen` lançait `ACTION_IMAGE_CAPTURE` **sans jamais demander l'autorisation caméra**, alors que le manifeste déclare `android.permission.CAMERA` pour le scanner. Android impose alors que l'autorisation soit accordée, faute de quoi il lève une `SecurityException` qui arrête le processus. Sur une installation neuve — précisément le cas après une réinstallation suivie d'une restauration — la première photo faisait tomber l'application ; elle fonctionnait si le gérant avait scanné un code-barres avant (cet écran demande l'autorisation correctement), d'où un plantage d'apparence aléatoire. L'autorisation est désormais demandée avant le lancement, avec un `try/catch` de dernier recours.
    - **Second défaut latent, corrigé au passage :** `TakePicturePreview()` fait transiter l'image par le résultat d'activité — certains appareils y renvoient la pleine résolution et dépassent la limite de transaction Binder (`TransactionTooLargeException`). Remplacé par `TakePicture(uri)` écrivant dans notre propre fichier via FileProvider. Effet secondaire notable : ce contrat ne rendait qu'une **vignette**, les photos produits étaient donc de très mauvaise qualité depuis le début. `files-path` a dû être ajouté à `file_paths.xml`, sans quoi `getUriForFile` sur `filesDir` lève `IllegalArgumentException` — un troisième plantage potentiel.
    - **Nouveau `util/PhotoStore`** : emplacement unique des photos (`filesDir/photos/`) et surtout **résolution tolérante** — chemin absolu, puis nom de fichier dans le dossier photos, puis racine de `filesDir` (emplacement historique). C'est ce qui rend une photo transportable : le chemin absolu enregistré en base désigne le dossier du téléphone d'origine et ne vaut rien après restauration, alors que le fichier est bien là, sous le même nom. `reduireApresCapture` calcule `inSampleSize` avant décodage : décoder 12 mégapixels pour les réduire ensuite demanderait ~48 Mo d'un coup.
    - **Nouveau `util/ArchiveUtil`** : archive `.zip` = `donnees.json` + `photos/`. Répartition assumée — **Firebase ne reçoit jamais les photos** (le JSON ne porte que les références `imageUrl`), parce qu'un envoi automatique après chaque mutation, payé au mégaoctet par l'épicier, ramenait le plan gratuit d'environ 80 boutiques à quelques-unes ; **tout le reste** (conservation, partage WhatsApp, dépôt Drive, carte SD) embarque les photos, ces envois étant manuels et occasionnels. La lecture accepte les deux formats, `.zip` et `.json` nu des versions précédentes. Protection **Zip Slip** : les noms d'entrée sont réduits à leur dernier segment et vérifiés contre le dossier autorisé — une archive arrive par WhatsApp, d'une main qui n'est pas toujours celle du gérant.
    - **Survie à la désinstallation :** `BackupHelper.deposerDansTelechargements` copie l'archive dans `Téléchargements/Varotra` via MediaStore (aucune autorisation de stockage requise depuis Android 10). Android efface intégralement le stockage privé d'une application désinstallée — base, photos et sauvegarde locale comprises, aucune application ne peut s'y soustraire : le dossier public est donc la **seule** façon de retrouver ses données après une désinstallation. Le partage y dépose systématiquement une copie.
    - **Références réécrites à l'import :** quand une photo est retrouvée localement (extraite de l'archive ou recréée depuis le base64), `syncFullDatabaseSync` réécrit `imageUrl` vers le fichier local. Sans cela l'image restait cassée à l'écran alors que le fichier était présent, et le chemin d'un téléphone étranger repartait dans la sauvegarde suivante. `photoDisponible` et `ImageBackupUtil` passent également par `PhotoStore`.
    - **Correctif suite à la revue de la PR #1 :** sur Android 9 et antérieur (`minSdk = 24`), le dépôt dans Téléchargements passe par un chemin direct qui exige `WRITE_EXTERNAL_STORAGE` — non déclarée. L'écriture échouait, `deposerDansTelechargements` renvoyait `false`, **et l'écran Paramètres ignorait ce résultat** : le gérant s'entendait annoncer une sauvegarde qui survivrait à la désinstallation alors qu'aucune copie n'existait. La permission est désormais déclarée avec `maxSdkVersion="28"` (jamais réclamée sur un téléphone récent, où MediaStore suffit), demandée au clic sur « Partager », vérifiée avant écriture, et le message final distingue les deux cas — copie durable écrite, ou partage seul.
    - **Non vérifié à la compilation dans l'environnement de développement** (SDK Android absent) : équilibrage syntaxique contrôlé, mais la compilation doit être validée par la CI ou un build local avant diffusion.

47. **Filet Global Anti-Plantage (`CrashReporter`) — A1**
    - **Problème visé :** un plantage ne laissait rien. L'application se fermait sèchement, le gérant appelait pour dire « ça s'est arrêté », et il n'existait aucun moyen de savoir où. Le plantage à la prise de photo (section 46) a coûté une enquête complète dans le code pour une cause qu'une seule ligne de pile aurait désignée. Une épicerie ne sait pas consulter un logcat, et personne n'ira brancher le téléphone en USB.
    - **Nouveau `util/CrashReporter`**, installé dans `MyApplication.onCreate` — au tout premier instant du processus, car posé dans l'Activity il laisserait sans surveillance la phase de démarrage, précisément là où une base corrompue ou une migration ratée se manifestent. Trois actions, dans cet ordre : (1) **sauver les données** via le crochet `actionUrgence` ; (2) **écrire le rapport** (date, version, appareil, fil, pile complète) sous `filesDir/crashs/`, cinq au maximum ; (3) **rendre la main au gestionnaire d'origine**, sans quoi l'application resterait figée sur un écran mort — pire que le plantage. Tout est enveloppé : le filet ne doit jamais devenir lui-même la cause d'un plantage.
    - **`InventoryViewModel.sauvegardeUrgenceSync`** : `flushBackupsNow` lance une coroutine, inutile dans un gestionnaire d'exception fatale — le processus meurt avant qu'elle ne démarre. La version synchrone écrit la sauvegarde locale sur le fil appelant, **sans envoi cloud** : une requête HTTP dans un processus condamné n'aboutit pas, et l'attendre transformerait un plantage en « ne répond pas ». Le crochet est branché dans `init` et libéré dans `onCleared`, pour ne pas laisser un ViewModel mort travailler.
    - **Côté gérant :** au démarrage suivant, après le splash — jamais avant, pour ne pas accueillir l'utilisateur par une erreur — une boîte de dialogue annonce que les données ont été sauvegardées et propose d'envoyer le rapport (partage Android, donc WhatsApp en pratique). Les rapports sont effacés à la fermeture de la boîte, qu'il ait envoyé ou non : reproposer le même message à chaque démarrage apprendrait au gérant à l'ignorer, et donc à ignorer le suivant.
    - **CI :** compilation et tests séparés en deux étapes. Quand une seule commande les enchaînait, un échec des tests rendait la case rouge sans qu'on sache si le code compilait — ce qui est arrivé dès le premier lot. Une étape affiche désormais le détail des tests en échec, le message de Gradle se limitant à « des tests ont échoué ».
    - **Dette diagnostiquée puis corrigée :** les cinq tests Robolectric du projet échouaient à l'amorçage sur `main` **avant** ce lot — établi par une branche témoin ne contenant que le workflow, dont la CI échouait à l'identique. Cause réelle, longtemps invisible faute d'un journal lisible : `Failed to create a Robolectric sandbox: Android SDK 36 requires Java 21 (have Java 17)`. Ni un Robolectric trop ancien, ni un `@Config(sdk = [36])` de trop — le JDK de la machine de CI. Corrigé en passant la vérification à Java 21 ; le projet continue de cibler Java 11 à la compilation.

48. **Coroutines du ViewModel sous Filet, et Échecs Rendus Visibles — A2**
    - **Problème visé :** onze `viewModelScope.launch { }` étaient dépourvus de gestionnaire d'exception, alors que le reste du ViewModel en passait un. Or ce sont les onze écritures critiques : encaissement d'une vente, enregistrement et suppression de produit, ajustement de stock, suppression de vente, création/remboursement/suppression de trosa, fournisseur, mises à jour de stock par synchronisation. Une exception dans l'une d'elles n'était rattrapée nulle part et fermait l'application — en pleine caisse.
    - **Le piège évité :** poser simplement le gestionnaire existant aurait remplacé un plantage par un **échec silencieux**, ce qui est pire ici. Le vendeur voit son panier se vider, croit la vente enregistrée, et ne découvre le trou qu'au comptage du soir. L'ancien gestionnaire n'écrivait que dans le logcat, invisible pour un épicier.
    - **`erreurOperation`** : nouveau canal d'état, alimenté par le gestionnaire et affiché par une boîte de dialogue dans `MainLifecycleContainer`. Une boîte, pas un message furtif : l'échec d'une écriture doit être constaté, pas aperçu.
    - **`lancerProtege(operation) { … }`** : remplace `viewModelScope.launch` sur ces onze sites. Le nom de l'opération voyage dans le contexte de coroutine (`CoroutineName`) et remonte dans le message, qui dit « l'enregistrement de la vente n'a pas abouti » au lieu d'« une erreur est survenue ». Sur un téléphone d'épicerie, la différence décide de ce que le gérant fait ensuite.
    - Aucun `viewModelScope.launch` nu ne subsiste dans le ViewModel ; les portées `GlobalScope` et `CoroutineScope(...)` ad hoc en étaient déjà absentes.

49. **Lecture Tolérante des Saisies Numériques (`NombreUtil`) — A3**
    - **Problème visé, et il ne plantait pas :** les saisies étaient lues par `toDoubleOrNull()`, qui n'accepte que le point décimal et aucun séparateur de milliers. Sur les 52 endroits où une saisie était lue, **deux seulement** rattrapaient la virgule (`replace(',', '.')`, dans les dialogues de quantité). Partout ailleurs, un gérant tapant `12,5` — ce que produit naturellement un clavier français ou malgache — obtenait `null`, aussitôt transformé en `0.0` par le `?: 0.0` qui suivait.
    - **C'est pire qu'un plantage.** Un montant devient zéro **sans rien dire**. Sur le champ « argent reçu » de la caisse, la monnaie rendue est fausse et personne ne s'en aperçoit avant le comptage du soir — et il est alors impossible de savoir quelle vente est en cause.
    - **Nouveau `util/NombreUtil`** : virgule ou point comme séparateur décimal, espaces de milliers y compris insécables (U+00A0, U+202F — ce que colle un presse-papier), devise en suffixe (`Ar`, `Ariary`, `MGA`), signe. Quand les deux séparateurs sont présents (`1.500,50` ou `1,500.50`), **le dernier est le décimal** : vrai des deux conventions, et évite de deviner la langue du clavier. Un point seul reste décimal, comme avant — le changer ferait basculer le sens des saisies déjà enregistrées.
    - **La tolérance porte sur la forme, jamais sur le contenu :** une saisie qui n'est pas un nombre renvoie toujours `null`. Renvoyer `0` rendrait une faute de frappe indiscernable d'un montant nul.
    - **Extensions `String?.enDecimal()` / `String?.enEntier()`** : la substitution des 55 sites se fait par simple changement de nom de méthode, sans réécrire les expressions appelantes — diff minimal, relecture possible. Les deux rattrapages manuels à la virgule deviennent superflus et sont retirés.
    - **`NombreUtilTest`, sans Robolectric** : 8 cas de test qui s'exécutent réellement dans la CI aujourd'hui, alors que les tests Robolectric du projet échouent à l'amorçage. La logique a par ailleurs été rejouée hors CI sur 21 cas avant livraison. Une régression ici ne se verrait pas à l'écran : elle produirait un montant faux.
50. **Correction — Comparaison Tolérante des Codes Appareil, Layout de l'Écran d'Activation**
    - **Bug réel signalé :** une licence à 2 appareils, tout « correct » dans Firebase (maxAppareils, ID), refusait pourtant le second téléphone (« licence utilisée sur un autre appareil »). Cause : `LicenceManager.verifierLicence` comparait le code appareil (`DeviceIdentity.rawId`, minuscules sans tirets) à la clé JSON de `appareils` par **égalité de chaîne stricte**. Le code affiché au client est `A3F2-9B10-C44D-E5F6` (majuscules, tirets) — c'est ce qu'il lit et transmet ; le fournisseur, en le recopiant dans la console, colle très naturellement cette forme telle quelle plutôt que d'appliquer mentalement la conversion documentée dans le guide. Résultat : correspondance jamais trouvée, appareil refusé, alors que tout semblait correct du point de vue du fournisseur.
    - **Correctif :** la comparaison normalise désormais les deux côtés — `normaliserCodeAppareil` ne garde que les caractères hexadécimaux en minuscules avant de comparer chaque clé de `appareils` au code de l'appareil courant. `A3F2-9B10-C44D-E5F6`, `a3f2-9b10-c44d-e5f6` et `a3f29b10c44de5f6` convergent désormais vers la même valeur. Couvert par `DeviceMatchingTest`.
    - **Écran d'activation — layout.** Le contenu (ID, code appareil, code hors-ligne, numéro de licence, bouton de vérification, message, aide) avait grossi au fil des versions sans jamais gagner de défilement : sur un écran modeste, ou dès que le clavier apparaissait pour saisir un champ, le bouton d'activation — en bas de la Column — se retrouvait rétréci ou poussé hors champ. `imePadding()` sur le `Box` externe réduit la hauteur disponible du clavier ; `verticalScroll` sur la `Column` interne (pas sur le `Box`, pour ne pas perturber son centrage) ne change rien au cas normal (la Column garde sa hauteur de contenu tant qu'elle tient à l'écran) et ne s'active que lorsque le contenu dépasse réellement l'espace disponible.

51. **Remboursement Groupé des Trosa d'un Même Débiteur (`DebtAllocation`) (Nouveau)**
    - **Demande utilisateur, avec exemple chiffré :** un client a plusieurs trosa distincts (ex. 2 300 Ar et 1 100 Ar, soit 3 400 Ar au total) et rembourse une somme qui ne correspond à aucune section précise (ex. 2 400 Ar). Jusqu'ici le gérant devait ouvrir chaque trosa un par un et répartir le montant à la main.
    - **Nouveau `util/DebtAllocation`** — fonction pure (aucune écriture, aucune dépendance Android), donc testable et réutilisable telle quelle pour l'aperçu à l'écran comme pour l'application réelle. Règle d'affectation : la dette au solde le plus élevé est soldée en priorité, le reliquat s'applique à la suivante par solde décroissant, jusqu'à épuisement du paiement ou de la dette totale ; à solde égal, la plus ancienne (date la plus faible) passe en premier. Un paiement supérieur au total dû laisse un reliquat non affecté (`montantNonAffecte`), jamais transformé en solde négatif. Couvert par `DebtAllocationTest` (7 cas, dont l'exemple exact 2 300 + 1 100, paiement 2 400 → soldée + 1 000 restant).
    - **`InventoryViewModel.repayDebtorGroup(debts, totalPayment)`** calcule le plan puis écrit chaque dette impactée en une seule opération protégée (`lancerProtege`), un seul déclenchement de synchronisation/sauvegarde pour tout le groupe plutôt qu'un par dette.
    - **`DebtorGroupCard`** gagne un bouton « Régler un montant sur l'ensemble », visible directement dans l'en-tête (pas besoin de déplier le détail) tant que le groupe n'est pas entièrement soldé. Ouvre `GroupRepayDialog`, qui affiche un **aperçu en direct** de la répartition au fur et à mesure de la saisie (quelle section se solde, ce qui reste) en utilisant EXACTEMENT le même calcul que celui appliqué à la confirmation — le gérant voit l'effet précis du montant avant de valider, aucune divergence possible entre l'aperçu et le résultat réel.
    - Un débiteur avec une seule dette n'est pas concerné (le remboursement simple existant reste inchangé) ; le remboursement individuel dette par dette (bouton "Rembourser" sur chaque `DebtCard`) reste disponible en complément pour un règlement ciblé sur une seule section.

52. **Plan d'Étagères Interactif (`EtagereScreen`, grille 2D extensible) (Nouveau)**
    - **Demande utilisateur, deux décisions tranchées explicitement avant codage (AskUserQuestion)** : (1) rendu **2D interactif** plutôt qu'une vraie scène 3D — un moteur 3D (Filament/SceneView) ajouterait des dizaines de Mo à l'APK et dépend d'un GPU capable, la première source de plantages sur le parc Android bas de gamme visé (minSdk 24) ; (2) **grille extensible** (lignes = niveaux d'étagère, colonnes = étagères) plutôt qu'un plan libre façon plan de magasin — correspond exactement à « gauche/droite » tel que décrit, et reste utilisable sur un petit écran de téléphone.
    - **Nouvelles entités Room** `Etagere` (position horizontale), `NiveauEtagere` (position verticale au sein d'une étagère — c'est « la case » cliquable), `ProduitNiveau` (lien many-to-many, un niveau contient couramment plusieurs références, un produit peut apparaître dans plusieurs niveaux). Migration **21 → 22**, purement additive (aucune colonne existante touchée), vérifiée par `Migration21To22Test` sur une base réelle.
    - **`util/EtagereLayout`** (fonction pure, testée : `EtagereLayoutTest`, 4 cas) : calcule la position d'un nouvel élément inséré à un bout ou l'autre d'une série (`min - 1` / `max + 1`, jamais un décalage des positions existantes) — ajouter une étagère ou un niveau ne déplace donc jamais ceux déjà en place, ni les produits qui y sont rangés.
    - **`InventoryViewModel.planEtagere`** reconstruit l'arbre étagères → niveaux → produits à chaque changement, par `groupBy` (O(n)) plutôt qu'un `filter` imbriqué (O(n×m)) — choix explicitement motivé par la demande d'optimisation, et ne tourne de toute façon que pendant que l'écran est affiché (`SharingStarted.WhileSubscribed`).
    - **`EtagereScreen`** : `LazyRow` d'étagères (seules celles visibles à l'écran sont composées — un magasin avec des dizaines d'étagères ne coûte pas plus cher qu'un magasin qui n'en a que trois), chacune une colonne classique de niveaux (nombre toujours modeste par étagère, un `LazyColumn` imbriqué n'aurait rien apporté). Boutons dédiés « à gauche »/« à droite » pour les étagères, « haut »/« bas » pour les niveaux d'une étagère donnée. Cliquer une case ouvre `NiveauDetailDialog` : renommage, retrait individuel d'un produit, recherche (en mémoire, catalogue de taille modeste) pour en ranger un nouveau, suppression du niveau — chaque suppression (étagère ou niveau) confirmée, jamais destructive pour le stock du produit lui-même.
    - **Intégré à la sauvegarde/synchronisation** (`SyncSerializer`, champs additionnels avec défaut `"[]"`, donc compatible avec les anciennes sauvegardes et les anciennes versions de l'app) : étagères et niveaux sont résolus par clé naturelle (nom+position) au lieu de l'id brut au moment de la restauration — même principe déjà appliqué à `LotProduit` — et les produits par code-barres/SKU/nom. **Simplification assumée** : contrairement aux entités financières (ventes, dettes...), pas de tombstones ni de propagation de suppression multi-appareils pour cette v1 — un plan d'étagères est un outil de rangement, pas un historique, le risque de conflit d'édition simultanée entre deux appareils y est faible, et l'effort a été concentré sur l'écran interactif lui-même.
    - Entrée dans Paramètres (« Plan d'étagères »), non soumise au verrou gérant/employé (au même titre que la fiche produit) : c'est un outil d'organisation, pas un historique financier ou d'audit.

53. **Rayon Picker depuis la Fiche Produit, et Deux Cas d'Erreur Anticipés sur le Plan d'Étagères (Nouveau)**
    - **Demande utilisateur :** « anticiper toutes sortes d'erreur » sur le plan d'étagères (52), puis pouvoir choisir l'emplacement en rayon d'un article **directement depuis sa fiche**, sans devoir passer par l'écran Étagère.
    - **Bug de fond corrigé — réattribution d'id après suppression :** `@PrimaryKey(autoGenerate = true)` de Room génère un simple `INTEGER PRIMARY KEY`, pas un `INTEGER PRIMARY KEY AUTOINCREMENT` — SQLite peut donc réattribuer l'id d'un produit supprimé au tout prochain produit inséré. Sans nettoyage, un produit sans aucun rapport avec l'ancien pouvait hériter silencieusement de son emplacement en rayon. `EtagereDao.deleteProduitsNiveauByProduit` est désormais appelé depuis `InventoryViewModel.deleteProduct`, dans la même opération protégée que la suppression elle-même.
    - **Bug d'affichage corrigé — dialogue figé et non défensif :** `EtagereScreen` gardait une référence figée (`niveauSelectionne: Pair<Etagere, NiveauAvecProduits>?`) au niveau ouvert dans `NiveauDetailDialog`, capturée au clic. Résultat : la liste des produits affichée dans le dialogue ne se mettait pas à jour après un ajout/retrait fait depuis ce même dialogue, et rien ne fermait le dialogue si le niveau disparaissait pendant que l'écran de plan était ouvert (multi-appareils qui se resynchronisent). Remplacé par un simple `niveauSelectionneId: Long?`, dont le niveau affiché est **recalculé** (`remember(plan, niveauSelectionneId)`) à chaque changement du plan, avec un `LaunchedEffect` qui referme le dialogue de lui-même si l'id ne se retrouve plus nulle part.
    - **`util/EtagereLayout.diffEmplacementUnique`** (fonction pure, 6 nouveaux cas dans `EtagereLayoutTest`) : calcule le diff à écrire pour qu'un produit n'ait plus qu'UN SEUL emplacement. À l'origine pensé comme un modèle plus simple réservé à la fiche produit (l'écran Étagère autorisait alors plusieurs emplacements par produit, ex. tête de gondole + rangement habituel) — **généralisé à tout le plan d'étagères dans la section 54** pour que les deux écrans restent toujours cohérents entre eux.
    - **`InventoryViewModel.saveProductAvecEmplacement(product, niveauChoisiId)`** remplace l'appel direct à `saveProduct` depuis `AddProductScreen` : enregistre le produit (insert ou update), **revalide que le niveau choisi existe encore** au moment d'enregistrer (protection contre l'écran resté ouvert pendant qu'un autre appareil supprime ce niveau par synchronisation — un id de niveau qui n'existe plus est traité comme aucun choix, jamais comme une erreur bloquante), puis applique le diff calculé par `diffEmplacementUnique`.
    - **`AddProductScreen`** : nouveau champ « Emplacement dans le plan d'étagères », une `OutlinedCard` cliquable (pas un `OutlinedTextField` + `.clickable` — le reste de l'écran utilise systématiquement `ExposedDropdownMenuBox` pour les sélections de ce type ; un `.clickable` sur un champ texte `readOnly` risquerait de voir le tap intercepté par la gestion tactile interne du TextField) ouvrant le sélecteur d'emplacement (voir `EmplacementPickerDialog`, mutualisé avec l'écran Étagère depuis la section 54) : niveaux groupés par étagère avec bouton radio, option « Aucun emplacement » en tête, et — si aucune étagère n'existe encore — un message explicite renvoyant vers Paramètres > Plan d'étagères plutôt qu'une liste vide sans explication.
    - `AddProductScreen.onSaveProduct` change de signature (`(Product) -> Unit` → `(Product, Long?) -> Unit`) ; les deux points d'appel (mise en page tablette et téléphone) dans `MainActivity` sont mis à jour en conséquence.

54. **Couleur des Rayons, Déplacement d'un Produit, Retrait Fluide, et Emplacement Unique Généralisé (Nouveau)**
    - **Demande utilisateur :** un color picker pour les étagères et les cases (« pour aider l'utilisateur » à s'y retrouver visuellement) ; corriger le retrait d'un produit d'une case au bouton « x », qui restait affiché jusqu'à la fermeture du dialogue alors qu'il était déjà supprimé en base ; une fonctionnalité pour déplacer un produit d'un rayon à un autre ; et — explicitement — que tout cela reste cohérent avec l'emplacement enregistré du produit.
    - **Couleur (`Etagere.couleur`, `NiveauEtagere.couleur`)** : hex `#RRGGBB` optionnel, normalisé par `util/EtagereColors.normaliser` (8 cas de test) avant d'être stocké ou affiché — une valeur corrompue (synchronisation partielle, ancien format, nom de couleur) est traitée comme absence de couleur plutôt que de risquer un crash à l'affichage. Migration **22 → 23**, additive (`ALTER TABLE ... ADD COLUMN ... DEFAULT NULL`), vérifiée par `Migration22To23Test` — les lignes existantes lisent `couleur = NULL`, jamais une valeur inventée. Sélecteur `ColorPickerDialog` (palette fixe de 12 couleurs, pas de roue chromatique — largement suffisant pour distinguer des rayons, et bien plus léger à l'écran) accessible depuis le menu `⋮` de chaque étagère et depuis un rond cliquable dans l'en-tête de `NiveauDetailDialog`. La couleur teinte l'en-tête de l'étagère et le fond de la case correspondante ; propagée dans les sauvegardes/synchronisation (`getFullDatabaseJsonSync` / `syncFullDatabaseSync`) comme les autres champs du plan d'étagères, avec valeur par défaut vide pour rester compatible avec les anciennes sauvegardes.
    - **Retrait fluide (bouton « x »)** : `NiveauDetailDialog` filtrait déjà `niveauAvecProduits.liens` en direct depuis le `plan` (section 53), mais le tap sur « x » attend l'aller-retour DAO → Flow Room → recomposition avant que la ligne disparaisse — sur un appareil modeste, un délai perceptible qui donnait l'impression que rien ne s'était passé avant la fermeture du dialogue. Un `Set<Long>` local (`idsEnCoursDeRetrait`, réinitialisé à chaque changement de niveau) filtre la ligne de l'affichage **au moment du tap**, avant même que l'appel à la base ne parte : le retrait est instantané à l'écran, et converge de toute façon vers le même état une fois la vraie donnée arrivée.
    - **Emplacement unique généralisé à tout le plan d'étagères :** `InventoryViewModel.ajouterProduitAuNiveau` (utilisé par la recherche de l'écran Étagère, et par tout futur point d'entrée) applique désormais `EtagereLayout.diffEmplacementUnique` avant d'écrire, exactement comme `saveProductAvecEmplacement` de la fiche produit (section 53). Un produit ne peut plus se retrouver dans deux cases à la fois : le choisir depuis la fiche produit et le voir depuis l'écran Étagère montre toujours le même emplacement, ce qui était le point explicitement demandé (« cohérent avec l'emplacement de l'article enregistré »). Cela remplace l'ancien modèle many-to-many de la v1 (tête de gondole + emplacement habituel) — assumé comme superflu face à l'exigence de cohérence, et jamais réellement exposé dans l'interface avant cette version.
    - **Déplacer un produit** : chaque produit rangé dans `NiveauDetailDialog` gagne une icône « ↔ » à côté du « x », ouvrant le même sélecteur d'emplacement que la fiche produit pour choisir une autre case. Comme `ajouterProduitAuNiveau` converge désormais vers un emplacement unique, déplacer n'est qu'un ajout vers la nouvelle case — l'ancienne est retirée automatiquement, sans code dédié. La recherche « ajouter un produit » affiche en prime, pour un résultat déjà rangé ailleurs, une légende « sera déplacé depuis... » : le gérant comprend qu'un clic déplace plutôt que duplique avant même de cliquer.
    - **`EmplacementPickerDialog`** : le sélecteur (niveaux groupés par étagère, bouton radio, option « Aucun emplacement » facultative via `autoriserAucun`) est mutualisé entre la fiche produit et l'écran Étagère plutôt que dupliqué — même composant, même comportement des deux côtés, ce qui est la garantie la plus directe que les deux écrans restent d'accord sur l'emplacement d'un produit.

55. **Correction de compilation (`insertProduct`), et Fiabilisation de la Vérification de Licence en Ligne**
    - **Build cassé sur `main` :** `InventoryRepository.insertProduct` a un corps de bloc (`{ ... }`) déclarant un retour `Int`, mais sa dernière ligne était une expression nue (`generatedId.toInt()`) au lieu d'un `return` explicite — contrairement à un corps d'expression (`= { ... }`), Kotlin ne transforme jamais automatiquement la dernière expression d'un corps de bloc en valeur de retour. `:app:compileReleaseKotlin` échouait donc avec « Missing return statement », jamais détecté par le harnais JVM isolé (qui ne compile pas ce fichier, dépendant de Room) ni par la simple vérification d'équilibre des accolades. Corrigé par un `return` explicite ; confirmé par une exécution CI réussie sur `main` juste après.
    - **Rapport signalé :** un client entre sa clé de licence et l'app ne s'active pas, alors que le fournisseur avait bien inscrit les 2 appareils et publié les règles Firebase. Investigation à distance sans accès à la base Firebase du fournisseur ni au téléphone du client — le point de départ a donc été de fiabiliser ce que le code peut garantir tout seul, plutôt que de deviner une cause précise côté configuration.
    - **Bug identifié par élimination :** `verifierLicenceEnLigne` positionnait `licenceEnVerification.value = false` (ce qui réaffiche le bouton normalement) AVANT `resultat.fold(...)`, qui écrit le message de résultat. Le fournisseur a confirmé exactement ce scénario après un test guidé : bouton revenu à la normale, mais aucun message affiché — cohérent avec une exception survenant après la réinitialisation du bouton mais avant l'écriture du message (ex. le `viewModelScope` interrompu par une mise en arrière-plan pendant l'attente réseau, plausible sur le parc bas de gamme visé et un réseau mobile lent). Un client verrouillé qui appuie sur ce bouton sans jamais rien voir se passer — ni activation, ni erreur — n'a alors plus aucun moyen de savoir s'il doit réessayer ou contacter le fournisseur.
    - **Correctif :** tout le corps de la coroutine est désormais sous `try/catch/finally` — `finally` garantit que le bouton redevient cliquable dans tous les cas, `catch` garantit qu'un message s'affiche même sur une exception totalement inattendue (une `CancellationException` reste relancée telle quelle, comportement normal de coroutine à ne jamais transformer en erreur visible).
    - **Message de résultat rendu impossible à manquer :** remplacé le petit texte gris (`bodySmall`, couleur `outline`) par une carte pleine couleur avec icône (vert + coche pour un succès, rouge + point d'exclamation pour un échec, via le nouveau `licenceMessageEstErreur`) — un résultat qui se fond dans le décor est un résultat qu'un client pressé ne remarque pas, au point de croire qu'il ne s'est rien passé du tout.
    - **Cause de configuration à vérifier séparément (hors code) :** le nœud Firebase doit être créé sous la clé EXACTE de l'ID d'installation à 6 chiffres du premier appareil du client (`/licences/<cet ID>`), avec `actif: true` et les deux codes appareil (normalisés côté app, donc tirets/majuscules tolérés) dans `appareils`. Une clé de nœud différente de cet ID (numéro de facture, numéro choisi à la main) fait échouer la recherche quel que soit le contenu de `appareils`.

56. **Messages de Restauration Cloud Précis (`ResultatRestaurationCloud`)**
    - **Rapport signalé :** après activation réussie des 2 appareils sur la licence, « la synchronisation des données ne marche pas ». Clarification obtenue : il s'agit de la restauration Firebase (code de récupération), pas de la synchronisation locale P2P (écran « Synchronisation multi-terminal ») — deux mécanismes entièrement distincts que le terme « synchronisation » désigne indifféremment pour un gérant non technique. Le gérant confirme avoir saisi le code de récupération du premier appareil sur le second (le geste attendu, voir section précédente sur le sujet), sans succès, mais sans retenir le message exact affiché.
    - **Défaut identifié à la lecture du code :** `FirebaseBackupManager.downloadBackup` fait remonter TROIS situations bien distinctes — code mal recopié, pas de réseau, aucune sauvegarde n'existe réellement sous ce code — sous un seul et même message « Aucune sauvegarde trouvée pour ce code ». Un gérant hors-ligne au moment de la restauration lirait ce message et en conclurait à tort que le code lui-même est faux, alors que le code était parfaitement correct.
    - **`InventoryViewModel.restaurerDepuisCloud(codeRecuperationSaisi)`** centralise ce que les DEUX points d'entrée de `SettingsScreen` (bouton « Restaurer » principal, et le dialogue « Nouveau téléphone ? ») dupliquaient à l'identique, et retourne un `ResultatRestaurationCloud` (`SUCCES` / `CODE_INVALIDE` / `HORS_LIGNE` / `AUCUNE_SAUVEGARDE`) plutôt qu'un simple `Result<String>` — la vérification réseau (`NetworkMonitor.isOnline`) est faite explicitement AVANT la tentative de téléchargement, pour distinguer nettement « pas de réseau » d'« aucune sauvegarde ». Chaque cas a désormais son propre message dans les trois langues (`messageResultatRestauration`), un gérant sait immédiatement s'il doit corriger le code, attendre une connexion, ou vérifier que le premier appareil a bien déjà sauvegardé.

57. **Rattrapage UI/UX — Skill `ui-ux-pro-max` et Audit de Cohérence des Écrans (En cours)**
    - **Skill installé** (`.claude/skills/ui-ux-pro-max/`, copié depuis `github.com/nextlevelbuilder/ui-ux-pro-max-skill`) : base de données interrogeable de styles, palettes, typographies, guidelines UX et règles spécifiques Jetpack Compose (`scripts/search.py`).
    - **Audit global** (grep sur `ui/screens`) : la fondation Material3 (`Theme.kt`/`Color.kt`, schémas clair/sombre complets, sélecteur de couleur d'accent) est saine, mais 247 couleurs et 257 tailles de police codées en dur contournent ces tokens à travers l'app, concentrées sur SalesHistoryScreen (51), SettingsScreen (34), CommissionScreen (33), HomeScreen (23), CalculatorScreen (23). Ce n'est pas que cosmétique : `SalesHistoryScreen` avait un fond racine `Color.White` codé en dur et plusieurs `if (isDark) Color(A) else Color(B)` dupliquant manuellement ce que `MaterialTheme.colorScheme` fait déjà tout seul — en clair, l'écran ignorait le mode sombre plutôt que de mal l'afficher.
    - **Politique de test décidée avec l'utilisateur** : plus aucun changement UI ne passe sur `main` sans être d'abord vérifié par la CI (tests unitaires + Robolectric/Compose) sur la branche de travail — ce sandbox n'a pas de SDK Android pour exécuter localement des tests Compose/Robolectric, seule la CI GitHub Actions le peut.
    - **`SalesHistoryScreen` (1er lot) :** tous les hex/isDark remplacés par `MaterialTheme.colorScheme.*` (onSurface/onSurfaceVariant/outlineVariant/surfaceVariant/error/background/surface selon le rôle), le fond racine suit désormais le thème. Laissés intentionnellement en dur : les couleurs de badge de catégorie (déco, pas des tokens de texte) et l'accent vert de profit positif — aucun token « succès »/« avertissement » n'existe encore dans le thème, à décider séparément plutôt que d'improviser un mapping. Trois `IconButton` de suppression/retour contraints à 24dp (sous le minimum tactile de 44dp) débridés vers la taille par défaut de `IconButton`. Nouveau `SaleListItemUiTest` (Robolectric + `createComposeRule`, suit le précédent `GreetingScreenshotTest`) vérifie l'affichage/dépliage en clair ET en sombre.
    - **Déclencheur CI ajouté** (`verification.yml`, `push: branches: ["claude/**"]`) : jusqu'ici la CI ne tournait que sur push/PR vers `main`, donc jamais sur une branche de travail — impossible de vérifier « tests avant fusion » sans ouvrir une PR juste pour ça. Ce même run a révélé que le harnais JVM isolé utilisé pendant la session ne fait tourner AUCUN test Compose/Robolectric — cette CI est donc la première exécution réelle de toute la suite `testDebugUnitTest` sur cette lignée de commits.
    - **Bug découvert par cette première exécution (`DeletionTombstoneTest`, préexistant, sans rapport avec le rattrapage UI) — deux diagnostics successifs :** 2 tests échouaient de façon intermittente. Premier diagnostic (insuffisant, voir plus bas) : `InventoryViewModel.deleteProduct` supprime la ligne produit PUIS enregistre le tombstone en deux appels `suspend` séquentiels distincts, et les tests n'attendaient que la disparition du produit avant d'enchaîner — corrigé en attendant l'existence du tombstone lui-même. **Un second run CI a de nouveau échoué avec ce correctif en place**, prouvant que ce n'était pas la vraie cause : le test attendait le tombstone jusqu'à 3 secondes complètes (30 tentatives × 100 ms) sans jamais le voir apparaître, ce qui exclut un simple problème de vitesse. **Vraie cause :** `lancerProtege` (donc `deleteProduct`) lance sur `viewModelScope` sans dispatcher explicite, donc `Dispatchers.Main.immediate` — sous Robolectric, `Main` est adossé au vrai Looper Android, dont la file reste **PAUSED** par défaut ; une continuation qui doit reprendre sur `Main` après un aller-retour par l'exécuteur interne de Room reste posée dans cette file tant que rien n'appelle explicitement `shadowOf(Looper.getMainLooper()).idle()` — ce qu'aucun test du projet ne fait. `syncFullDatabaseSync` (utilisé par `BackupRestoreDuplicationTest`, resté fiable) échappe au problème car il lance explicitement sur `Dispatchers.IO`, qui n'a besoin d'aucun Looper pompé. **Correctif définitif :** `Dispatchers.setMain(UnconfinedTestDispatcher())` dans un `@Before` (+ `Dispatchers.resetMain()` en `@After`) sur `DeletionTombstoneTest` ET `BackupRestoreDuplicationTest` (les deux seuls tests qui construisent un `InventoryViewModel` réel) — un dispatcher non confiné exécute les continuations immédiatement sur le thread qui les reprend, sans jamais dépendre d'un Looper pompé. Repère utile pour la suite : **tout nouveau test qui construit un `InventoryViewModel` et attend l'effet d'un appel passant par `lancerProtege` doit installer ce même dispatcher de test**, sous peine de retomber sur exactement ce piège.

58bis. **Mise à Jour Interne via Cloudflare R2 — CI cassée deux fois de plus après le push initial, corrigée avant `main`**
    - Avant de fusionner la section 58 sur `main`, deux nouveaux échecs CI (branche `claude/**`) ont dû être corrigés en plus de la flakiness de `DeletionTombstoneTest` : (1) `gitVersionCode` calculé via `ProcessBuilder("git", "rev-list", ...)` directement dans `app/build.gradle.kts` — le cache de configuration de Gradle (activé par défaut, Gradle 9.3.1 sur ce projet) rejette tout processus externe lancé pendant la configuration (« Starting an external process ... is unsupported »), ce qui cassait la compilation ELLE-MÊME, avant même d'atteindre les tests. Corrigé avec `providers.exec { commandLine(...) }.standardOutput.asText.get()`, l'API Provider compatible avec le cache de configuration — **toute future lecture d'un processus externe depuis un fichier `build.gradle.kts` de ce projet doit passer par `providers.exec`, jamais par `ProcessBuilder`/`Runtime.exec` direct**. (2) La flakiness de `DeletionTombstoneTest` documentée ci-dessus a nécessité un second diagnostic (`Dispatchers.setMain(UnconfinedTestDispatcher())`) après qu'un premier correctif se soit révélé insuffisant sur un second run CI.
    - Fusionné sur `main` par fast-forward (`72980ac..e9fdea8`) une fois la CI verte sur la branche de travail — aucun commit divergent sur `main` entretemps, donc pas de vrai merge nécessaire.

59. **Rattrapage UI/UX — `SettingsScreen` (2e lot)**
    - Même traitement que `SalesHistoryScreen` (section 57) : les 4 variables `cardBg`/`cardBorderColor`/`mainTextColor`/`secondaryTextColor`, jusque-là dérivées d'un `isDark = MaterialTheme.colorScheme.background == Color(0xFF002114)` comparé à un hex précis, redéfinies directement avec les tokens Material3 (`surface`/`outlineVariant`/`onSurface`/`onSurfaceVariant`) — noms de variables conservés tels quels (71 sites d'usage dans ce fichier, dont plusieurs paramètres passés à `VendeurRolesCard`) pour ne pas devoir toucher chaque site d'appel individuellement.
    - Les 6 sélecteurs par puces (mode clair/sombre/système, mode boutique détail/gros) partageaient le même couple de ternaires `if (isDark) Color(0xFF1B4332) else Color.White` / `if (isDark) Color(0xFFCBD5E1) else Color(0xFF475569)` pour l'état non sélectionné — remplacés par `MaterialTheme.colorScheme.surfaceVariant`/`onSurfaceVariant` via `replace_all` (littéraux identiques aux 6 sites).
    - Le contour de la pastille de couleur sélectionnée dans le sélecteur de thème (`if (isDark) Color.White else Color.Black`) remplacé par `MaterialTheme.colorScheme.onBackground`, qui offre le même contraste sans comparaison manuelle.
    - Deux rouges « danger » incohérents entre eux (`0xFFD32F2F` pour la carte Sécurité & Surveillance, `0xFFC62828` pour la suppression d'un compte employé) unifiés sur `MaterialTheme.colorScheme.error` — les deux étaient déjà utilisés de façon cohérente au sein de leur propre bloc, donc `replace_all` sûr à chaque fois.
    - Le petit toast (`Card` flottante en bas d'écran, jusque-là toujours `Color(0xFF1E293B)`/`Color.White` peu importe le thème) migré vers `MaterialTheme.colorScheme.inverseSurface`/`inverseOnSurface` — le couple de tokens Material3 conçu précisément pour un élément qui doit rester contrasté quel que soit le thème courant (au lieu d'une couleur sombre codée en dur qui perdait justement ce contraste en mode sombre).
    - Volontairement laissés en dur, même règle qu'en section 57 : les 4 couleurs d'accent du sélecteur de thème (`emerald`/`sunset`/`indigo`/`rose`, ce sont les valeurs mêmes du thème, pas des couleurs d'interface) et les deux verts de succès (`0xFF2E7D32` texte, `0xFF4ADE80` icône du toast) — aucun token « succès » dans le thème pour l'instant.
    - Deux `IconButton` (modifier/supprimer un compte employé) contraints à 32dp (sous le minimum tactile de 44dp) débridés vers la taille par défaut de 48dp. Le bouton retour circulaire à 40dp laissé tel quel : choix de design délibéré (cercle visible, proche du minimum), pas le même genre d'oubli que les 24dp de la section 57.
    - Nouveau `SettingsScreenUiTest` (Robolectric + `createComposeRule`, même structure que `SaleListItemUiTest`) : construit un vrai `InventoryViewModel` (Room en mémoire + `Dispatchers.setMain(UnconfinedTestDispatcher())`, voir section 58bis) et vérifie que l'écran s'affiche en clair ET en sombre, sans jamais cliquer sur le bouton de vérification de mise à jour (un vrai clic déclencherait un appel réseau sortant vers R2 dans un test unitaire — même règle déjà suivie pour les boutons Firebase de cet écran).

60. **Rattrapage UI/UX — `CommissionScreen` (3e et dernier lot des 3 pires écrans)**
    - Contrairement à `SalesHistoryScreen`/`SettingsScreen`, cet écran n'avait **aucune tentative** de mode sombre (pas de variable `isDark` du tout) — fond racine et fond de la barre d'onglets tous les deux `Color.White` codé en dur, donc entièrement illisible en mode sombre (fond blanc, texte clair par défaut du thème sombre). Les deux migrés vers `MaterialTheme.colorScheme.background`.
    - Même mapping de rôle que les deux lots précédents, par `replace_all` (chaque littéral vérifié à tous ses sites d'usage avant remplacement, pour ne pas mélanger deux rôles sous un même hex) : `0xFF1E293B`→`onSurface` (texte principal, y compris la valeur par défaut du paramètre `color` de `CalculationRow` — un paramètre par défaut PEUT appeler une propriété `@Composable` comme `MaterialTheme.colorScheme.*` du moment que la fonction elle-même est `@Composable`, ce qui est le cas ici) ; `0xFF64748B`/`0xFF475569`→`onSurfaceVariant` (texte secondaire) ; `0xFFF1F5F9`→`surfaceVariant`, `0xFFF8FAFC`→`surface`, `0xFFE2E8F0`/`0xFFCBD5E1`→`outlineVariant` (fonds/bordures de cartes claires) ; `Color.Red`→`error` (indicateurs de perte/bénéfice négatif, 3 sites).
    - **Piège évité — un même hex, deux rôles différents :** `0xFFEF4444` apparaissait à deux endroits avec un besoin différent : dans `marginStatusColor` (badge de statut de marge, texte seul sur fond neutre) → `MaterialTheme.colorScheme.error` ; dans le badge « stock sous le seuil » (fond `0xFFFEE2E2` + texte `0xFFEF4444`, donc un vrai couple conteneur/contenu) → `errorContainer`/`onErrorContainer`, la paire de tokens Material3 faite pour ce genre de badge doux plutôt qu'un `error` plat sur un fond neutre. Traités individuellement plutôt que par un `replace_all` qui aurait mélangé les deux rôles. De même pour le petit toast de succès en bas d'écran (`0xFF1E293B`/`Color.White`, même schéma que les sections 57/59) : corrigé individuellement en `inverseSurface`/`inverseOnSurface` AVANT le `replace_all` général de `0xFF1E293B`→`onSurface`, pour ne pas l'y engloutir.
    - Volontairement laissés en dur, même règle que les lots précédents : les verts positifs (`0xFF10B981`, `0xFF059669`, bénéfice/marge positive, coche « stock complet »), l'amber d'avertissement (`0xFFF59E0B`, marge faible — aucun token « avertissement » dans le thème) et l'icône décorative de l'ampoule-conseil (`0xFFEAB308`, unique occurrence).
    - Aucun `IconButton` sous le minimum tactile cette fois — le seul bouton contraint en taille (le retour circulaire à 40dp) suit le même choix de design délibéré que les sections 57/59.
    - Nouveau `CommissionScreenUiTest` (même structure que les deux précédents) vérifie l'affichage en clair ET en sombre via le bouton `validate_commission_restock_button`.
    - **Les 3 écrans les plus problématiques de l'audit initial (section 57) sont désormais migrés** : `SalesHistoryScreen`, `SettingsScreen`, `CommissionScreen`. Le reste de l'app (`HomeScreen`, `CalculatorScreen`, etc.) garde encore des couleurs codées en dur — non traité dans ce lot, à reprendre séparément si demandé.

58. **Mise à Jour Interne de l'App via Cloudflare R2 (`UpdateManager`) (Nouveau)**
    - **Demande utilisateur :** que l'app puisse se mettre à jour elle-même (sans que le client refasse toute l'installation), l'APK signé étant hébergé sur un bucket Cloudflare R2 (`varotra`, URL publique `https://pub-8562b104a83841e09966f730d5763b79.r2.dev`). Confirmé explicitement : la mise à jour reste **toujours facultative**, jamais forcée au démarrage.
    - **Blocage préalable corrigé — `versionCode`/`versionName` codés en dur :** `app/build.gradle.kts` fixait `versionCode = 1` / `versionName = "1.0"` sans jamais les incrémenter — Android refuse catégoriquement d'installer par-dessus une app existante un APK dont le `versionCode` n'est pas **strictement supérieur** à celui déjà installé (sans quoi il faudrait désinstaller). Remplacé par `gitVersionCode = git rev-list --count HEAD` (nombre de commits sur la branche) : ne peut que croître d'une release à l'autre, contrairement à un compteur qu'on oublie d'incrémenter à la main. `versionName = "1.0.$gitVersionCode"`. `release.yml` doit checkouter l'historique complet (`fetch-depth: 0`) pour que ce compte soit exact — un clone superficiel (profondeur par défaut = 1) renverrait toujours 1.
    - **`util/UpdateManager`** (même style qu'`util/FirebaseBackupManager`/`LicenceManager` : OkHttp brut + `org.json.JSONObject`, pas de SDK ni de dépendance ajoutée) : lit le manifeste `latest/version.json` du bucket, télécharge `latest/varotra.apk` avec suivi de progression, vérifie son empreinte SHA-256 si le manifeste en fournit une (fichier supprimé et rejeté si elle ne correspond pas), puis lance l'installateur système via `FileProvider` (déjà configuré au projet, `files-path` couvre le téléchargement stocké dans `filesDir`). La comparaison/décision de proposer une mise à jour (`parserManifesteMiseAJour`) est une fonction pure séparée de l'appel réseau, testée par `UpdateManagerTest` (versions plus récente/égale/plus ancienne, manifeste sans `apkUrl`, JSON corrompu, empreinte SHA-256 connue) — même principe que `EtagereColors.normaliser`. Comme pour `FirebaseBackupManager`/`LicenceManager`, la partie réseau elle-même n'a pas de test Compose/intégration dédié (précédent établi dans ce projet : ces appels REST ponctuels ne sont pas rejoués en CI, seule leur logique pure l'est).
    - **Permission ajoutée** (`REQUEST_INSTALL_PACKAGES` au manifeste) : nécessaire mais pas suffisante — sur Android 8+, l'installation depuis une app tierce exige EN PLUS une autorisation par-app accordée via l'écran système (`Settings.ACTION_MANAGE_UNKNOWN_APP_SOURCES`), vérifiée par `UpdateManager.autorisationInstallationAccordee` avant de tenter `lancerInstallation` ; si elle manque, l'utilisateur est renvoyé vers cet écran plutôt que de voir l'installation échouer silencieusement.
    - **UI (`SettingsScreen`, nouveau `UpdateCard`)** : bouton « Vérifier les mises à jour » → bannière colorée façon activation de licence (section 55) si déjà à jour ou en cas d'erreur réseau, ou carte dédiée avec version/notes + bouton « Télécharger » (barre de progression) puis « Installer » une fois le fichier prêt. Écrit avec les tokens Material3 (`MaterialTheme.colorScheme.*`) directement — c'est un composant neuf, pas de dette de couleurs codées en dur à reproduire ici même si le reste de `SettingsScreen` n'a pas encore été migré (section 57, en cours).
    - **`release.yml`** gagne une étape « Publier sur Cloudflare R2 », après la signature de l'APK : calcule `versionCode`/`versionName` (même formule que Gradle), l'empreinte SHA-256 de l'APK signé, publie `latest/varotra.apk` et `latest/version.json` (tous deux `Cache-Control: no-store`, pour qu'un cache en périphérie ne serve jamais un ancien APK sous la même URL après une nouvelle release) via l'API S3-compatible de R2 (`aws s3 cp --endpoint-url https://<ACCOUNT_ID>.r2.cloudflarestorage.com`). **Étape volontairement non bloquante** : si les secrets `R2_ACCOUNT_ID`/`R2_ACCESS_KEY_ID`/`R2_SECRET_ACCESS_KEY` ne sont pas encore configurés côté GitHub, elle s'arrête proprement (`exit 0`) sans faire échouer le reste de la release. Ces credentials R2 sont scopés à un jeton API créé côté Cloudflare (recommandé : restreint au bucket `varotra` uniquement) — le compte (`R2_ACCOUNT_ID`) est partagé par tous les buckets d'un même compte Cloudflare, mais les clés d'accès dépendent du scope choisi à la création du jeton.

61. **`release.yml` — Distribution de l'APK via une Release GitHub plutôt qu'un Artefact de Build**
    - **Rapport signalé :** « le build a échoué ». Diagnostic : le build release lui-même réussissait (10 min, APK signé produit), mais l'étape `actions/upload-artifact@v4` échouait avec « Artifact storage quota has been hit » — le stockage d'artefacts de build GitHub Actions est un quota séparé (recalculé toutes les 6-12h), déjà rencontré plus tôt dans la session sur `verification.yml` (l'upload du rapport de tests en cas d'échec, non bloquant celui-là car conditionné par `if: failure()`), et n'est de toute façon pas fait pour distribuer durablement un livrable final.
    - **Corrigé en remplaçant cette étape par `softprops/action-gh-release@v2`** : attache l'APK en pièce jointe d'une vraie Release GitHub (onglet Releases du dépôt), un stockage entièrement distinct sans ce quota, disponible immédiatement et durablement — y compris avant même que les secrets R2 optionnels soient configurés. Nécessite `permissions: contents: write` au niveau du job (sinon le `GITHUB_TOKEN` par défaut, en lecture seule sur certains dépôts, refuserait la création). Tag et nom de release dérivés de `versionName` (`v1.0.<versionCode>`), calculé une seule fois dans une nouvelle étape « Calculer la version » et partagé via `$GITHUB_ENV` avec l'étape Release et l'étape R2 (qui le recalculait auparavant en double).
    - **Validé par `workflow_dispatch`** sur la branche de travail avant de toucher `main` : `release.yml` accepte un déclenchement manuel (`on: workflow_dispatch`) sur n'importe quelle réf, ce qui a permis de vérifier ce correctif sans passer par un vrai push sur `main` — utile pour tout futur changement à ce workflow, qui ne se déclenche autrement que sur `main`/`master`.
    - **Deuxième bug découvert en cascade — `assertExists` inconnu :** en essayant de corriger `SettingsScreenUiTest`/`CommissionScreenUiTest` (qui échouaient avec « The component is not displayed! » — un écran complet en `fillMaxSize()` + défilement rendu dans `createComposeRule()` sans Activity hôte, dont la fenêtre de test n'a pas de dimensions garanties sous Robolectric), le premier réflexe — remplacer `assertIsDisplayed()` par `assertExists()` — a cassé la COMPILATION des tests (« Unresolved reference 'assertExists' », `androidx.compose.ui.test.assertExists` ne résout pas dans la version de Compose de ce projet). Corrigé pour de bon en gardant `assertIsDisplayed()` (déjà prouvé fonctionnel par `SaleListItemUiTest`) et en enveloppant le contenu testé dans un `Box(Modifier.size(400.dp, 2400.dp))` à l'intérieur de `setContent { }`, pour donner à la fenêtre de test Robolectric des dimensions concrètes et généreuses — assez hautes pour que tout le contenu défilant (y compris un bouton de validation loin dans le formulaire, comme sur `CommissionScreen`) soit considéré « affiché » sans geste de défilement explicite dans le test.
    - **Troisième tentative, la bonne — le `Box` de taille explicite ne suffisait pas non plus :** une fois la compilation corrigée, `assertIsDisplayed()` échouait ENCORE avec « The component is not displayed! », malgré le `Box(Modifier.size(400.dp, 2400.dp))` censé garantir des bornes concrètes — preuve que le problème n'était pas la taille de la fenêtre mais son ABSENCE : `createComposeRule()` sans Activity hôte ne fournit pas de vraie fenêtre/decor view sous Robolectric pour un écran racine en `fillMaxSize()`. Corrigé avec `createAndroidComposeRule<ComponentActivity>()` (déjà une dépendance du projet via `androidx.activity.compose`) à la place de `createComposeRule()` nu — héberge le contenu dans une vraie Activity Robolectric, la méthode standard pour tester un écran complet plutôt qu'un composant enfant auto-dimensionné. Avec une fenêtre réaliste (hauteur d'écran de téléphone standard), certains éléments visés (`update_check_button` en bas de `SettingsScreen`, `validate_commission_restock_button` assez bas dans le formulaire de `CommissionScreen`) tombent hors du premier écran visible : ajouté `.performScrollTo()` avant `.assertIsDisplayed()`, qui fait défiler jusqu'à l'élément exactement comme le ferait un utilisateur, plutôt que de supposer qu'il est toujours visible sans défilement.
    - **Repère pour la suite :** tout nouveau test Compose qui rend un écran ENTIER (pas un composant enfant auto-dimensionné comme `SaleListItem`, qui reste sur `createComposeRule()` nu) doit utiliser `createAndroidComposeRule<ComponentActivity>()`, et appeler `.performScrollTo()` avant `.assertIsDisplayed()` sur tout élément qui n'est pas garanti dans le tout premier écran visible.

62. **Rattrapage UI/UX — `HomeScreen` (4e écran, au-delà des 3 pires de l'audit initial)**
    - **Analysé avec le skill `ui-ux-pro-max`** (grep ciblé plutôt que lecture intégrale des 3800 lignes cumulées de `HomeScreen`/`CalculatorScreen`, contre les guidelines `jetpack-compose.csv` et `ux-guidelines.csv`) avant de coder quoi que ce soit, à la demande explicite de l'utilisateur. Verdict : `HomeScreen` (629 lignes, un seul composable) a le bug de mode sombre le plus visible du rattrapage à ce jour — `containerColor = Color.White` codé en dur sur CHAQUE carte de la grille produits (l'écran que le gérant regarde en continu à la caisse), sans même la tentative (fautive) de `isDark` qu'avaient `SalesHistoryScreen`/`SettingsScreen` au départ. `CalculatorScreen` (3174 lignes, un seul composable de ~2870 lignes) est nettement plus gros et plus risqué à traiter d'un coup — laissé de côté pour l'instant, à reprendre séparément avec une passe de découpage en sous-composables avant toute migration de couleurs.
    - **Décomposition** (guideline stack Compose #2 « small composables », #33 « extract reusable layout patterns ») : `HomeScreen` avait deux copies QUASI IDENTIQUES du même bloc (logo/marque, bouton Paramètres, champ de recherche, en-tête de section) — une pour l'état vide (aucun produit), une pour l'en-tête à défilement collant. Extrait en `HomeHeaderContent`, appelé aux deux endroits : un futur changement de couleur ou de libellé ne se fait plus qu'à un seul endroit, alors qu'avant cette section 62 il aurait fallu se souvenir de le répéter. La carte produit de la grille (auparavant ~190 lignes inline dans le `LazyVerticalGrid`) extraite en `ProductGridCard`, sur le même principe que `SaleListItem` pour `SalesHistoryScreen` : état/callbacks en paramètres (`cartQuantity: Double?` plutôt que l'objet panier complet), aucune dépendance directe au ViewModel — réutilisable et testable isolément.
    - **Couleurs** : `Color.White` du conteneur de carte produit → `MaterialTheme.colorScheme.surface` (LE correctif dark-mode de cette section) ; `Color(0xFF94A3B8)`/`Color(0xFF64748B)` → `onSurfaceVariant` ; `Color(0xFFF1F5F9)` → `surfaceVariant` ; `Color.Black` (texte du champ de recherche, prix hors gros) → `onSurface` ; `Color(0xFF1E293B)` → `onSurface` ; fond de l'image produit placeholder (`0xFFE2E8F0`/`0xFFECEFF1`) → `surfaceVariant`, et le `ColorPainter` d'erreur de chargement d'image Coil (`0xFFFFCDD2`) → `errorContainer`.
    - Volontairement laissés en dur, même règle que les sections précédentes : le lien accent « Izy rehetra / Voir tout » (`0xFFF57C00`, décoratif, distinct de `themeColor` — un changement de couleur ici serait un choix de design, pas un simple remplacement par un token) et le badge « Total: X » (`0xFFFFECE0`/`0xFFD35400`, même statut que les badges de catégorie déjà laissés en dur sur `SalesHistoryScreen`/`CommissionScreen`) ; les `Color.White` en `tint`/`contentColor` sur fond `themeColor` (icône du panier, badge de quantité dans le panier) suivent le même pattern déjà établi.
    - Aucun `IconButton` sous le minimum tactile — le bouton Paramètres (40dp, circulaire) suit le même choix de design délibéré que les sections précédentes.
    - Nouveau `HomeScreenUiTest` : contrairement aux écrans précédents, `HomeScreen` a deux chemins de rendu réellement différents (état vide / grille avec produits) plutôt qu'un seul — les tests vérifient `home_settings_button` et `search_input` (rendus par `HomeHeaderContent`, donc présents dans les deux chemins) en clair ET en sombre, sans dépendre de la course entre l'insertion explicite d'un produit dans le test et le seed asynchrone du `InventoryViewModel` à la construction.

63. **PIN Gérant : Corriger l'Oubli Aléatoire de la Demande, Ajouter une Réinitialisation par Code de Récupération (Nouveau)**
    - **Rapport signalé :** « parfois, en ouvrant l'app et en allant directement dans Paramètres via l'icône de l'accueil, le PIN gérant n'est pas demandé ».
    - **Cause exacte :** `_activeVendeurId` (donc `activeVendeur`, donc `estGerant`/`requiresGerantPinForSettings`) est initialisé au tout premier accès depuis `appPreferences.activeVendeurId`, une valeur PERSISTÉE sur l'appareil. Un gérant qui s'était connecté un jour et n'avait jamais explicitement cliqué « Déconnexion » restait donc reconnu comme gérant indéfiniment — y compris après une fermeture complète de l'app et un nouveau lancement (nouveau processus). Le « parfois » du rapport, c'était très précisément : ça dépend de qui était l'utilisateur actif la dernière fois que l'app a été fermée, ce qui semble aléatoire vu de l'extérieur.
    - **Corrigé avec un drapeau de session JAMAIS persisté** (`gerantSessionVerifiedThisLaunch`, réinitialisé à `false` à chaque construction du `InventoryViewModel`, donc à chaque ouverture fraîche de l'app) : `estGerant` exige désormais À LA FOIS que le compte actif soit un gérant ET que ce drapeau soit `true` — mis à `true` uniquement par une saisie de PIN réussie PENDANT l'exécution en cours (`verifyGerantPin`, `loginVendeur` si le compte est un gérant, ou `reinitialiserPinGerant` après réinitialisation), et remis à `false` par `logoutVendeur`. `requiresGerantPinForSettings()` se simplifie en un simple `!estGerant`, puisque le cas « aucun compte » (pas de PIN requis) est déjà couvert par `estGerant`.
    - **Corrigé de façon cohérente partout, pas seulement à l'entrée de Paramètres :** `estGerant` gate aussi `peutSupprimerHistorique()` (suppression de ventes/dettes/retours/mouvements de caisse/réappros) — laisser CE contrôle sur l'ancien `activeVendeur` seul aurait laissé le même trou de sécurité grand ouvert pour toute suppression sensible, simplement déplacé hors de Paramètres plutôt que corrigé.
    - **Nouvelle fonctionnalité demandée — PIN oublié :** un lien « PIN oublié ? » sur le dialogue de PIN gérant (`MainActivity`) ouvre `ResetGerantPinDialog`, en deux étapes : (1) saisie du **code de récupération de la boutique** (`RecoveryCode`/`AppPreferences.firebaseBackupToken`, déjà utilisé pour la sauvegarde Cloud, déjà en possession du gérant) — la seule preuve d'identité vérifiable hors-ligne sans dépendre d'un AUTRE gérant déjà connecté, justement indisponible si le gérant unique de la boutique est celui qui a oublié son PIN ; (2) une fois le code validé, choix du compte gérant à réinitialiser (radio, uniquement si plusieurs comptes gérant existent) puis un nouveau PIN + confirmation. `InventoryViewModel.reinitialiserPinGerant` re-vérifie le code (defense-in-depth, même principe que `saveProductAvecEmplacement` re-validant ses préconditions), connecte immédiatement le gérant après succès (pas de ressaisie du PIN qu'il vient de choisir), et journalise l'événement (`AuditLog.Type.REINITIALISATION_PIN`, sévérité `ATTENTION` — un reset de PIN est un geste sensible qui mérite sa propre trace, pas noyé dans `CONNEXION`).
    - Nouveau `GerantPinSessionTest` : reproduit EXACTEMENT le bug signalé (écrit `activeVendeurId` dans les préférences AVANT de construire le `InventoryViewModel`, comme le ferait un vrai redémarrage à froid) et vérifie que le PIN est bien redemandé ; couvre aussi `logoutVendeur`, la validation du code de récupération (formats variés : tirets, minuscules, espaces — via `RecoveryCode.normalize`) et le cycle complet de réinitialisation (ancien PIN révoqué, nouveau PIN actif, mauvais code refusé sans rien changer).

64. **Rattrapage UI/UX — `CalculatorScreen` (l'écran de caisse, le plus gros et le plus utilisé)**
    - **Le chantier explicitement reporté par la section 62** (« nettement plus gros et plus risqué à traiter d'un coup — à reprendre séparément avec une passe de découpage en sous-composables avant toute migration de couleurs »), repris ici dans cet ordre : décomposition d'abord, couleurs ensuite.
    - **Décomposition** : la ligne d'article du panier était dupliquée à l'IDENTIQUE (~120 lignes chacune) entre la disposition tablette (deux panneaux côte à côte) et la disposition téléphone (onglet « checkout ») — seule la couleur d'accent différait entre les deux copies. Extraite en `CartItemRow`, sur le même principe que `SaleListItem` (section 57) et `ProductGridCard` (section 62) : état et callbacks en paramètres (`onQuantityDelta`/`onEdit`), aucune dépendance directe au ViewModel. Le composable principal passe de ~2870 à ~2690 lignes.
    - **Découpage des bornes vérifié par comptage de accolades, pas à l'indentation** : l'indentation de ce fichier est incohérente par endroits (blocs sur-indentés hérités, `contentDescription = null` désalignés) et ne peut pas servir à repérer de façon fiable la fin d'un bloc à extraire. Les bornes exactes des deux copies à remplacer ont été calculées en comptant les accolades depuis le `{ item ->` de chaque `items(cart)`, et l'équilibre global accolades/parenthèses re-vérifié après chaque édition — indispensable sur un fichier de cette taille qu'on ne peut pas compiler localement (voir plus bas).
    - **Couleurs : 71 → 25 littéraux codés en dur.** Deux VRAIS bugs de mode sombre corrigés : (1) `containerColor = Color.White` sur chaque carte produit de la caisse (`CashRegisterProductCard`) — exactement le même bug que `HomeScreen` en section 62, sur l'autre écran que le gérant regarde en continu ; (2) `Color.White` comme couleur de contenu de la carte de facturation remplie en `colorScheme.primary` — en mode sombre `primary` est une teinte CLAIRE, donc du blanc sur clair y devenait illisible ; remplacé par `onPrimary` sur toute l'étendue de cette carte (bornes calculées, pas devinées). Tous les rouges de danger (`Color.Red`) → `colorScheme.error`, et les textes du dialogue scanner posés sur la surface du dialogue (`0xFF334155`, `0xFF64748B`, `0xFFEF4444`) → tokens.
    - **Les 25 littéraux restants sont volontaires**, même règle que les sections précédentes : `Color.Transparent` de bordure de champ, `Color.White` en `tint`/`contentColor` sur aplat `themeColor`, la maquette délibérément sombre du viseur caméra (fond noir, laser vert, coches vertes — elle doit rester sombre dans les deux thèmes, c'est une simulation de viseur), et les accents décoratifs (bouton ambre `0xFFFFB300`, pastille bleue `0xFF8CC1F0`, vert de « rendu suffisant » `0xFF2E7D32` pour lequel Material3 n'a pas de token `success`).
    - **`themeColor` et `MaterialTheme.colorScheme.primary` sont la MÊME couleur à l'exécution** : `MainActivity` appelle `MyApplicationTheme(primaryColorOverride = themeColor)`, qui fait `baseScheme.copy(primary = primaryColorOverride)`. Unifier `CartItemRow` sur `themeColor` alors que la copie téléphone utilisait `colorScheme.primary` ne change donc RIEN visuellement — vérifié avant de fusionner les deux copies, précisément parce que fusionner deux blocs qui diffèrent par une couleur n'est sans risque que si cette différence est illusoire. En revanche l'onglet « divers rapide » utilisait un vert CODÉ EN DUR (`0xFF2E7D32`) pour `PaymentModeRow` et son bouton de validation, alors que les deux autres points d'entrée suivaient déjà le thème : il suit maintenant `themeColor` comme les autres — seul changement visuel réel de cette section, à vérifier à l'œil.
    - **`IconButton` sous le minimum tactile : volontairement NON traités dans ce lot.** L'audit initial en signalait 7 (de 16dp à 36dp) sur cet écran. Les agrandir dans des rangées de caisse très denses est un changement de mise en page dont l'effet ne peut PAS être vérifié ici : la CI ne fait que compiler et lancer les tests unitaires, et le build local est impossible dans cet environnement (le proxy réseau ne donne pas accès au dépôt Maven de Google, `com.android.application` ne se résout pas — voir plus bas). À reprendre quand un rendu visuel réel sera disponible, plutôt qu'à l'aveugle.
    - **Impossible de compiler localement dans cet environnement** : `./gradlew` échoue en configuration avec « Plugin [id: 'com.android.application', version: '9.1.1'] was not found » — le proxy sortant ne donne pas accès au dépôt Maven de Google. Toute vérification de compilation passe donc obligatoirement par la CI (`verification.yml`), ce qui rend le contrôle manuel d'équilibre accolades/parenthèses décrit plus haut d'autant plus important avant chaque push.
    - **Troisième cas de figure du « component is not displayed », distinct des deux de la section 61 :** le premier run CI de `CalculatorScreenUiTest` a échoué ainsi alors que la compilation passait et que les 73 autres tests étaient verts. Ni un problème d'Activity hôte (déjà réglé section 61) ni un manque de défilement : la disposition de caisse est une `Column` NON défilante dont le contenu à hauteur FIXE (en-tête + sélecteur de produits 231dp + carte de facturation ~186dp ≈ 520dp) dépasse le petit écran par défaut de Robolectric, donc le bas est rogné — et `performScrollTo()` ne peut RIEN y faire faute de conteneur défilant. Corrigé avec `@Config(qualifiers = "w411dp-h891dp")`, qui fixe un gabarit de téléphone réaliste et haut. **Repère : la largeur doit rester sous 600dp**, sinon `CalculatorScreen` bascule sur sa disposition tablette à deux panneaux (`screenWidthDp >= 600`), qui ne porte pas ces `testTag`. Le même `qualifiers` est appliqué à `SyncDebtsScreenUiTest` pour rendre ces tests indépendants de l'écran par défaut de Robolectric, susceptible de changer d'une version à l'autre.
    - Nouveau `CalculatorScreenUiTest` (recette de la section 61 : `createAndroidComposeRule<ComponentActivity>()` + `Dispatchers.setMain(UnconfinedTestDispatcher())`) : vérifie l'écran en clair ET en sombre. Deux `testTag` ajoutés pour cela — `calculator_product_search` et `calculator_checkout_button` sur la disposition TÉLÉPHONE, qui n'en avait aucun (seule la disposition tablette était taguée, alors que le téléphone est le chemin par défaut). Pas de `performScrollTo()` ici, contrairement à `SettingsScreen`/`CommissionScreen` : la disposition de caisse est une `Column` NON défilante (le panier absorbe l'espace restant via `weight(1f)`), donc `performScrollTo()` y échouerait faute de conteneur défilant.

65. **Rattrapage UI/UX — `SyncScreen` et `DebtsScreen`**
    - **`SyncScreen` portait encore la comparaison `isDark` défectueuse** déjà corrigée sur `SalesHistoryScreen`/`SettingsScreen` : `val isDark = MaterialTheme.colorScheme.background == Color(0xFF002114)`, qui compare le fond du thème à un littéral figé. Dès qu'une palette sombre change de fond (ou qu'un `primaryColorOverride` en modifie la dérivation), la comparaison devient fausse et TOUT l'écran repasse en couleurs claires posées sur un fond sombre. Remplacé par les quatre mêmes tokens que `SettingsScreen`, en gardant les mêmes noms de variables (`cardBg` → `surface`, `cardBorderColor` → `outlineVariant`, `mainTextColor` → `onSurface`, `secondaryTextColor` → `onSurfaceVariant`) : aucun site d'utilisation à modifier, seule la définition change. 20 → 12 littéraux.
    - **`DebtsScreen` mélangeait deux écritures pour la MÊME sémantique** : `Color(0xFFD32F2F)` et `MaterialTheme.colorScheme.error` cohabitaient, parfois dans le même ternaire, pour « dette en retard / impayée ». Le rouge codé en dur passe à `error` là où il colore un texte seul, et à la paire `errorContainer`/`onErrorContainer` pour le badge « EN RETARD » (fond + texte), même distinction que celle déjà faite sur `CommissionScreen` en section 57. 16 → 10 littéraux.
    - **Restent volontairement en dur**, même règle que les sections 62 et 64 : le vert « payé/succès » `0xFF2E7D32` (aucun token `success` en Material3, déjà conservé sur `CalculatorScreen`), l'orange d'avertissement `0xFFEF6C00`, la palette d'état de connexion de `SyncScreen` (vert/bleu/ambre/rouge — un jeu de couleurs sémantique cohérent façon feu tricolore, qu'on casserait en n'en migrant qu'une seule), le fond noir du viseur de scan QR et le widget « console » de logs (carte bleu nuit, police monospace, rouge/vert/gris de terminal) qui doit rester sombre dans les DEUX thèmes, exactement comme le viseur caméra de `CalculatorScreen`.
    - **Déséquilibre de parenthèses préexistant dans `DebtsScreen`** (662 `(` pour 663 `)`, dans une chaîne de caractères) : présent avant toute modification, donc le contrôle d'équilibre décrit en section 64 se fait ici en COMPARANT au fichier d'origine (`git show HEAD:...`) plutôt qu'en visant l'égalité absolue — la vérification utile est que l'édition retire autant d'ouvrantes que de fermantes (ici −8 / −8).
    - Nouveau `SyncDebtsScreenUiTest` (recette de la section 61), 4 tests : les deux écrans en clair ET en sombre. Deux `testTag` ajoutés à `SyncScreen`, qui importait `testTag` sans jamais l'utiliser (`sync_back_button`, `sync_status_card`) ; `DebtsScreen` avait déjà les siens (`debt_search_input`, `add_debt_button`, tous deux inconditionnels et en haut de l'écran, donc sans `performScrollTo()`).

66. **Rattrapage UI/UX — `PeremptionScreen`, `DashboardScreen`, `CaisseMouvementsScreen`, `BarcodeListScreen` + correction du bouton de validation rogné**
    - **Les QUATRE écrans portaient la même comparaison `isDark` défectueuse** que `SyncScreen` (section 65) : `MaterialTheme.colorScheme.background == Color(0xFF002114)`. C'est le bug de mode sombre le plus répandu du projet — il touchait 7 écrans au total. Corrigé partout de la même façon, en conservant les noms de variables (`mainTextColor` → `onSurface`, `secondaryTextColor` → `onSurfaceVariant`, `cardBg` → `surface`, `cardBorderColor` → `outlineVariant`) pour ne toucher que les définitions. 52 → 29 littéraux sur les quatre fichiers.
    - **Deux boutons d'action destructrice réparés au passage** (`PeremptionScreen` « Supprimer », `CaisseMouvementsScreen` « Fermer la caisse ») : ils fixaient `containerColor = Color(0xFFC62828)` SANS `contentColor` associé. `ButtonDefaults.buttonColors` dérive alors le contenu via `contentColorFor(containerColor)`, qui ne connaît pas les couleurs hors palette et retombe sur la couleur ambiante — d'où un risque de libellé sombre sur rouge sombre. Remplacés par la paire `error`/`onError`, qui garantit le contraste dans les deux thèmes.
    - **Restent volontairement en dur** : les triplets d'état « feu tricolore » (`0xFFC62828` / `0xFFF57C00` / `0xFF2E7D32` pour périmé / bientôt périmé / bon, écart de caisse, marge), qu'on casserait en n'en migrant qu'une branche ; le langage de couleur des montants du tableau de bord (vert = positif, rouge = négatif) laissé cohérent d'un bout à l'autre de l'écran plutôt que migré à moitié ; les bandeaux pleins à contenu blanc explicite (`PeremptionScreen`, en-tête rouge quand des lots sont périmés), où passer à `error` réintroduirait précisément le bug blanc-sur-clair corrigé en section 64.
    - **Bouton le plus bas inatteignable — la vraie cause n'était pas celle qu'on croyait.** L'utilisateur signalait « parfois les écrans n'affichent pas le bouton le plus bas » en demandant d'ajouter un défilement à l'écran d'activation. Or `ActivationScreen` avait DÉJÀ été corrigé (`verticalScroll` + `imePadding`, commit `c4b7053`, déjà sur `main`) : ce symptôme-là ne pouvait venir que d'un APK antérieur au correctif. En revanche l'audit déclenché par ce signalement a trouvé le vrai cas restant, et c'est le plus grave possible : **le bouton « Valider » de la caisse**. La disposition téléphone de `CalculatorScreen` est une `Column` NON défilante (le panier absorbe l'espace via `weight(1f)`) suivie de blocs RIGIDES — sélecteur de produits (~231dp) puis carte de facturation (~186dp) qui porte le bouton. Le déclencheur en usage réel n'est pas le petit écran mais le CLAVIER : on l'ouvre pour saisir les espèces reçues, la fenêtre se réduit d'autant, le total rigide dépasse la place restante et le bouton sort de l'écran — d'où le « parfois ».
    - **Corrigé avec `BoxWithConstraints` + `imePadding()` posé sur le Box LUI-MÊME** (et non sur la `Column` interne) : c'est la condition pour que `maxHeight` reflète la place réellement disponible, clavier compris. En dessous de 620dp utiles, le sélecteur de produits se comprime (175dp → 104dp) et la carte de facturation reste entièrement visible. Une constante tirée de `LocalConfiguration.screenHeightDp` n'aurait RIEN corrigé ici : cette valeur décrit l'écran physique et ne bouge pas à l'ouverture du clavier.
    - **Le correctif est prouvé par un test, pas supposé** : `le bouton de validation reste visible quand la hauteur disponible est reduite` rend l'écran en 411x460dp (l'ordre de grandeur qu'il reste d'un téléphone courant clavier déployé) et exige `assertIsDisplayed()` sur `calculator_checkout_button`. Ce test échouerait sans la compression du sélecteur — c'est la seule façon de vérifier une correction de mise en page dans ce dépôt, le build local étant impossible (section 64) et la CI ne rendant aucune capture.

67. **Fin du rattrapage UI/UX — `SecuriteScreen`, `InventoryListScreen`, `AddProductScreen`, `EtagereScreen`**
    - **Constat le plus utile de ce dernier lot : il restait très peu de dette réelle.** Sur les 14 littéraux de ces quatre écrans, la majorité relève des palettes sémantiques déjà conservées volontairement dans les sections 64 à 66 — la palette de sévérité de `SecuriteScreen` (ALERTE rouge / ATTENTION orange / INFO bleu, plus le rouge « action bloquée » qui en fait partie), le couple stock faible / stock correct de `InventoryListScreen`, le FAB ambre assorti à la barre du bas, le brun décoratif des étagères et le `Color.Gray` de repli quand la couleur d'étagère choisie par l'utilisateur n'est pas analysable. Aucun de ces cas n'est un oubli : les migrer casserait un jeu de couleurs cohérent ou une intention de design. `SecuriteScreen` et `EtagereScreen` n'ont donc AUCUNE modification.
    - **Vraie incohérence trouvée dans `InventoryListScreen`** : les badges « ALERT » et « LOW QTY », côte à côte et visuellement identiques, utilisaient deux rouges DIFFÉRENTS — l'un `MaterialTheme.colorScheme.error`, l'autre `Color(0xFFD32F2F)` codé en dur. Pire, le badge déjà tokenisé associait `error` à `color = Color.White` : en thème sombre `error` est une teinte CLAIRE, donc du blanc dessus devient illisible — exactement le bug corrigé sur la carte de facturation en section 64. Les deux badges passent à la paire `error`/`onError`, ce qui les rend identiques ET lisibles dans les deux thèmes.
    - **`AddProductScreen`** : les deux `ColorPainter` de Coil migrés comme en sections 62 et 64 (`surfaceVariant` pour le placeholder — il rejoint ainsi la couleur du `Box` qui l'entoure, déjà tokenisée — et `errorContainer` pour l'échec de chargement). L'écran ne contient plus aucun littéral de couleur.
    - **Bilan du rattrapage** (sections 57, 62, 64, 65, 66, 67) : les 12 écrans de l'audit initial sont traités. Le gain principal n'est pas le décompte de littéraux mais l'élimination de la comparaison `isDark` défectueuse, présente sur 7 écrans, et de la famille de bugs « contenu blanc sur un token clair en thème sombre », trouvée sur `CalculatorScreen` puis `InventoryListScreen`. Restent hors périmètre et assumés : les ~257 tailles de police codées en dur (jamais commencées) et les `IconButton` sous le minimum tactile de `CalculatorScreen` (section 64), tous deux nécessitant un rendu visuel réel pour être traités sans régression.

68. **Formulaire d'ajout d'article — les catégories personnalisées créées via « Hafa » reviennent dans la liste déroulante**
    - **Rapport utilisateur :** « si je clique la catégorie "Hafa" puis saisis la nouvelle catégorie, elle doit être incluse dans la liste déroulante pour les autres articles ».
    - **Ce qui marchait déjà, et ce qui manquait :** la catégorie personnalisée était bien ENREGISTRÉE sur le produit — à l'enregistrement, `finalCategory` reprend la saisie libre dès que « Hafa » est sélectionné, et Room la voyait donc (`getAllCategories()` fait un `SELECT DISTINCT category`). Le trou était uniquement à l'affichage : la liste déroulante itérait sur `standardCategories`, une liste FIGÉE en dur dans le composable. La catégorie créée existait en base mais n'était jamais reproposée — il fallait la ressaisir à l'identique pour chaque article suivant, avec le risque de créer des doublons (« Pièces moto » / « Pieces moto ») à la moindre variation de frappe.
    - **Corrigé en réinjectant les catégories déjà en base** : `viewModel.categories` (qui existait déjà et alimente le filtre par catégorie ailleurs dans l'app) est collecté ici, filtré de ce que `standardCategories` contient déjà, trié, et concaténé à la liste. **« Hafa » est explicitement remis en DERNIER** : ce n'est pas une catégorie réelle mais l'entrée « autre » qui fait apparaître le champ de saisie — la laisser au milieu de la liste après ajout des catégories personnalisées aurait été déroutant.
    - **Volontairement NON modifié : l'initialisation en mode édition.** Un produit dont la catégorie est personnalisée continue d'ouvrir le formulaire sur « Hafa » avec le champ libre prérempli. Sélectionner directement la catégorie dans la liste aurait semblé plus propre, mais aurait retiré la possibilité de CORRIGER le libellé depuis l'écran d'édition — et la liste des catégories arrive de façon asynchrone, donc l'état initial ne peut pas en dépendre de façon fiable.
    - Nouveau `AddProductCategoryDropdownTest` : enregistre un produit avec une catégorie personnalisée, ouvre la liste déroulante d'un NOUVEAU produit et exige que la catégorie y figure. Premier run CI en échec sur « The component is not displayed! », mais avec une information précieuse : les DEUX `waitUntil` étaient passés (sinon l'erreur aurait été un timeout), ce qui prouvait que `viewModel.categories` contenait bien la catégorie ET qu'elle était rendue dans le menu — le correctif applicatif était donc bon, seule l'assertion finale échouait. Cause : les catégories personnalisées sont ajoutées APRÈS la trentaine d'entrées standard, donc l'entrée était hors de la portion visible du menu. Corrigé par `performScrollTo()` avant `assertIsDisplayed()`, même recette que la section 61. Deux points de synchronisation, car rien ici n'est immédiat : on attend d'abord que `viewModel.categories` contienne la valeur (le `StateFlow` est en `WhileSubscribed`, c'est le `collectAsState()` de l'écran qui déclenche la requête Room), puis que l'entrée du menu soit rendue après le clic. `performScrollTo()` avant le clic, le formulaire étant long et défilant.
